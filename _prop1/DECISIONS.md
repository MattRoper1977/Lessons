# PROP-1 — the owner's ruling on the PROPOSED set, recorded verbatim

**Sentinel:** `prop-1-2026-08-20-TOP` / `prop-1-2026-08-20-BOTTOM`
**Ruled:** 2026-08-20. **Source of truth for items:** `_sca1close/PROPOSED_RANKED.md` at
`e63f047` — 323 rows, 284 `apply` / 28 `hold` / 11 `withdraw`. This pass does not restate
the exact changes; the table carries them. What is recorded here is the ruling and the law.

---

## 0. Scope of the ruling — verbatim

> 1. **APPLY every row the table marks `apply` (284)**, using each row's "exact change" text.
>    Directional rulings inside that set: A-48/XP26 = ADD lundy (and feedback where present)
>    to the print lists — never delete the sections; A-P112 = amend the SoW kit box to the
>    supervised-bench wording (option 1), the deck stays; A-P24 = the one-string fix
>    ("The apple and the orange in your lunch").
> 2. **APPLY these previously-held rows** (rulings): XA14 · XP1/XA13 · A-64 (no prior cohort
>    exists — apply each neutralised wording) · XA22 (interim wording) · XO2 (fix the brief,
>    SoW stands) · A-58 ("Complete the Unit") · A-59 ("Bronze Part D scope") · A-40
>    ("Next term:") · XA1 ("the printed tiles") · XP34 (deck canonical, SoW changes; XP18 =
>    no deck change) · A-P149 (align pack to decks: carousel/paired) · XA18 (add Paragraph 3
>    slot; **★assessed — commit message must cite "R-A01 owner sign-off 2026-08-20"**) · B-1 ·
>    B-6 · SCA-P3 Branch A (publish the command-word set, 0 deck edits) · SCA-P6a · SCA-P6b.
> 3. **HOLD, untouched:** A-4, A-7, A-13, A-22, A-50, XA21 (module-code family — owner is
>    confirming codes; leave every current string), A-P118, A-P126 (adviser ruling pending),
>    A-47 (Spring SoW), the BUILD-HUM oral close / W7 written lines (sentinel-held by design).
>    **SCA-P4 is DECLINED** — record as closed-no-action.
> 4. **WITHDRAWALS (12): execute each row's closing action** (strike/replace the doc lines as
>    written), nothing else.

## -1a. Ruling addendum (2026-08-20) — verbatim

> **A-P118 and A-P126 move from HOLD to APPLY.** The Arts Award adviser confirmed Art W3
> evidences **Explore Parts A + C** — the deck is canonical. Align the
> Printable_BUILD_Weekly_Evidence pack's W3 sheet stamp and the locator's Part rows to
> "Explore Parts A + C", matching the deck's title strip/KO/evidence line, and reconcile W2
> per the row's note, one commit, same gates as every other family.

So the applied set is **284 + 17 (§0.2) + 2 (§-1a) = 303 rows**, and the residual hold list is
**A-4 · A-7 · A-13 · A-22 · A-50 · XA21 · A-47** plus **XP18** (ruled "no deck change") and
**SCA-P4** (declined, closed-no-action), and the sentinel-held BUILD-HUM oral close / GROW +
LAUNCH HUM W7 written lines.

---

## Judgement calls made inside the ruling

These are the places where the ruling did not fully determine the bytes. Each is recorded so
the decision is visible rather than silently absorbed.

### J1 · The EQA-visit octet wording was normalised to one sentence
`A-17`'s source entry proposes *"…the first-year EQA sampling **activity** must be booked
**within the first delivery year — confirm with the coordinator**."* Its six siblings
(`A-51`, `A-52`, `A-53`, `A-55`, `A-56`, `A-57`) all propose *"…the first-year EQA sampling
**visit** must be booked **during this first delivery year (coordinator action)**."*
Shipping both would leave the estate self-inconsistent about an accreditation fact — the
exact defect class this pass exists to close — and §1 batches this set as **one family**.
The six-row majority wording was applied to all seven. `A-57` additionally restores the
`IQA before EQA;` clause its panel variant had dropped, as its row directs.

### J2 · WITHDRAWALS: the ruling says 12, the table carries 11
§0.4 states "WITHDRAWALS (12)". `PROPOSED_RANKED.md` marks **11** rows `withdraw`, and its own
category header agrees (F 0 · C 8 · D 0 · J 3 = 11). All 11 closing actions were executed.
No twelfth withdrawal row exists in the table to execute. Reported, not invented.

### J3 · An 8th file carries the EQA claim and no row names it
Measured estate-wide, `the first-year EQA sampling visit is booked` appeared in **8** files.
The table names **7** (`A-17`, `A-51`, `A-52`, `A-53`, `A-55`, `A-56`, `A-57`); the eighth,
`LAUNCH_ASDAN/PEQ/START_HERE.html`, has no row. §3 puts "anything not in the table" out of
scope, so it was **left unedited and reported** rather than swept silently. It is the one
place in the estate still asserting a booking the committed compliance record keeps open.
One line closes it, on the owner's word.

### J4 · SCA-P7 edits a §5-protected answer-key attribute, by name
`SCA-P7`'s exact change is literally `data-h="..." -> data-h="..."`, and its own row says it
"sits on a §5-protected surface". §1 of the ruling allows this where a row names the
protected string and cites the ruling, which this one does. Only the *hint* value moved:
the scored key `data-c="0"` and the explanation `data-e` are byte-identical, so no scored
control was re-keyed. The SCA instrument still measures **468 protected strings across 12
families** with `answer-key-data` at 166 — the count did not move, only that one hash.

The replacement wording is a judgement call the owner should sanity-check. The ranked
table truncates the new string at `data-h="Balance is a`, and the estate holds two
candidates: `_sca1/PROPOSED.md` P7 proposes "not **equal-sized** sections", while the
earlier raw artifact `_sca1/tables/findings_raw.csv` proposes "not **four** identical
sections". The curated form was applied, because the id chain is
`findings_raw.csv → PROPOSED.md → PROPOSED_RANKED.md` and the ranked id `SCA-P7` is
literally PROPOSED.md's section number. It is also the better fix on the merits: dropping
the count entirely stops the hint contradicting the four-group plate the pupil is looking at,
which is the whole defect.

### J5 · SCA-P2 was reshaped, and its insertion text had to be reconstructed
The ranked table truncates SCA-P2 at "insert immediately before `<div class="print-route
supported">` a line mirroring the sc". The anchor exists exactly once, so the target is not
in doubt; the mirrored text is what the truncation removed.

Reading the reshaped row against the bytes settles it. The row says the original rationale
was wrong — and it is: "record the force in newtons (N)" is **not** confined to the
teacher-facing PRACTICAL READY block, it is in the pupil-facing Independent task box on
screen. What is genuinely missing is the **print** mirror: all three printed route boxes
give the task with no unit guidance at all. So the residual defect is print parity, and the
fix is to mirror the screen's own sentence into the pack.

The inserted line therefore quotes the screen verbatim and invents nothing:
"**Measuring:** Use an agreed scale for this. Use a spring balance if you have one and
record the force in newtons (N)." The earlier P2 proposal also wanted "centimetres (cm)" on
the distance; that phrase appears on no surface in the file, so it was **not** added — it
would be authoring, not mirroring.

### J6 · XO2's ruling lands on a document this repo does not hold
§0.2 rules XO2 "fix the brief, SoW stands". The row itself proposes the opposite (edit
`Humanities_Teesside/LAUNCH_Scheme_of_Work.html` line 8); the owner has inverted it, so the
SoW subtitle naming AQA UAS is **correct and untouched**.

The "brief" is `BRIEF_HUM`, an input to the ECA-1 audit rather than a committed file — it
does not exist anywhere in this repository (measured: the phrase "the SoW names no board"
appears only inside `_eca1/PROPOSED_A.md` and `_sca1close/PROPOSED_RANKED.md`, quoting it).
There is therefore no brief here to edit. XO2 is closed by this record: **the SoW stands,
and the brief's "names no board" premise is superseded.** No file changed.
