# HC1 HEALTH — 2026-09-06 (Lessons; estate-wide sections)

Token: `mbm-two-domain-healthcheck-2026-09-06-HC1` · verdict **HC1_PARTIAL**.
Map: `docs/ESTATE_MAP_2026-09-06.md` (same commit). Companion ledgers with the same
name sit in `docs/` of the Site, Games and Apps repos on branch `claude/hc1-p6-ledger`.
Every number below was measured on the four publication trees rebuilt from the exact
pinned sources (map §3) and on CI logs read through the Actions API; the domains
themselves are unreachable from this venue (proxy 403), so "served" claims cite CI.

Mains at measurement: Lessons `904a3c1a` · Site `d9115711` · Games `ec5ee7bb` · Apps `96d160f0`.

## P0 — ORIENT · HC1_P0_OK
Written up in the map. R12: two files are published in full on both domains by written
ruling (`Lesson_VIR_Pupil_App.html`, `R_Gate_Calibration_Game.html`). Judged documented,
not ambiguous; measurement continued; **no self-merge anywhere** (all PRs HELD) until
Matt confirms the map.

## P1 — SPLIT INTEGRITY · HC1_P1_OK (measured; corrections held)

**1.1 Leak census (education domain).** 1282 full pages classified. Game-loop
heuristic (rAF + canvas/WebGL + score/lives/game-over) flagged 16, all read at the
call site: 11 Lessons decks (quiz score-bars, Art slideshow JSON, Tutor Time, liveteach
projector) = LESSON; 4 Apps science investigations = APP (ruled educational in
`education_policy.py`); `/artsaward/` = tool. **Games served from madebymatt.uk: 0.**
UNCLEAR for Matt: none beyond the R12 pair.

**1.2 Hardcoded origins.** Served trees: play still contains `https://madebymatt.uk`
in 8 files — all "Back to Education" links (CORRECT); education contains
`madebymatt-play.uk` only in `game-saves.js`, `usage-client.js`, `usage-dashboard.js`,
`education-expansion.json` (CORRECT: links/showcase). Source: the 31 `Games/*.html`
canonical/og:url tags cite `madebymatt.uk/Lessons/Games/…` (CORRECT in source; the play
builder rewrites the host, and on education the file is a stub). STALE code/config:
the Site live verifiers listed under 2.5, and Lessons `wave-ohm-deck-live.yml` +
`glv3-production-byte-check.yml` (dispatch-only; compare raw source bytes to served,
but served lessons now carry the injected navigation adapter — they will red).

**1.3 Old-URL continuity.** 73 pre-split game routes (Site index 72 ∪ Lessons
resources 31 ∪ shelf 62): 67 → stub on education, full on play; 3 → stub whose link
targets a legacy alias that exists on play (`Trail_Runner`→`/trailrunner/`,
`Voxel_Frontier`→`/voxel/`, `Trekkers_Trail_Runner (2)`→`Trekkers_Trail_Runner_Tees_Coast`);
1 full on education only (`Lesson_VIR_Intervention`, ruled educational); 2 full on both
(R12 pair). Printed/QR citations: LundyLoop poster QR pattern
`madebymatt.uk/Lessons/LundyLoop/3_subject_guides/{subject}.html` (education, still
served); Science teaching packs cite `SCI_B_W3…W7`, `SCI_G_W3…W7` (served); liveteach
QR encodes its own origin. **Cited old URLs without a landing: 0.** Stubs: 1.1–1.2 KB,
noindex, one "Open the game" link + save-export + pupils link; **no `rel=canonical`**
to the play URL (Tier 2, Site builder `moved_page()`).

**1.4 Origin-scoped state.** Map §5. Expected: passport (play only), account/Members
(education only), `mbm_reading_theme` per origin. Broken-by-construction and handled:
game saves — export at `/game-saves/` (education) / import at `/game-saves/` (play),
allowlist byte-pinned. No lesson reads a play-only key. Config outside the repos:
none needs changing (Supabase and FormSubmit stay on `madebymatt.uk`).

**1.5 Cross-origin dependencies.** Education pages that LOAD from play: **0**
(site 0, lessons 0, apps 0). Play pages that LOAD from education: **0**. Education →
play links: site 24 pages / 40 links, lessons 6 hub pages / 6 links, apps 1; all
labelled. Play → education: 4 home variants ("Back to Education"). CSP: six
`Humanities_Teesside/GROW_W9-W14_2026-27/*OUTSTANDING_V3_1.html` decks carry
`default-src 'none'; script-src 'unsafe-inline'` which **blocks the builder's injected
`/Lessons/assets/catalogue/lesson-navigation.js` and `/assets/usage*.css|js`** (console
errors on the served pages; Aut2 W9–14, not this half-term). `/evidence-binder/moderator-pro/`
CSP blocks `/assets/education-support.css`. Tier 2, interpretive (relax CSP or skip
injection) — MATT list.

**1.6 Service workers.** Education: none (none existed pre-split in Site, Lessons or
Apps source, so no stale SW can pin old game routes). Play: `micro-tinkerer/sw.js` only,
game-scoped; precache resolution not verified from here.

**1.7 Search / index / hubs.** Education search index 677 entries, 0 game/play routes;
education `domain-catalogue.json` 765 + 564 rows, 3 external (GitHub Planning links).
Play catalogue: 62 games + 6 activities + 1 staff, own routes only. `/for/pupils/` on
education is a learning page (Primary, Science pathways, resources; 3 labelled play
links) — not a games shelf. Every published lesson gets the navigation adapter (way home
`/Lessons/`).

**1.8 Hygiene.** canonical/og:url on-domain everywhere measured (site 41, lessons 1,
apps 2, play 86). Education sitemap 424 locs (412 `/Lessons/`, 1 Apps), 0 game/play;
play sitemap 70 own locs. robots: education `Disallow: /next/`; play allow all. CNAME:
education root `madebymatt.uk`, play `madebymatt-play.uk`; Lessons/Apps trees carry none
(project mounts — correct). HTTPS enforced: **not measurable here**; CI measured play
HTTPS 200 on `/data/domain-catalogue.json` (Games run 34038125168) and education over
https (Site provenance run 34044012989). http→https redirect and `www.madebymatt.uk`:
NOT MEASURED. Mixed content: 0 (no `http://` subresources in any tree).

**1.9 Blockability (education).** Third-party subresources on pupil-reachable routes
(static): `fonts.googleapis.com` 56 pages, `youtube-nocookie.com` iframes 51,
`phet.colorado.edu` 3, `cdn.tailwindcss.com` 2, `cdn.jsdelivr.net` 2, `cdnjs` 1
(`PDF_Studio`). At runtime 17 routes attempted an external request (12 × 2025-26
library decks, 2 Tutor Time, 2 primary Y4, 1 Physics). Pre-existing; **none added**.
Analytics: none (usage client is first-party, opt-in, off by default). Ko-fi: links only,
26 site adult pages, 16 Apps adult tools, **155 Lessons pages including the pupil-reachable
hubs `/Lessons/`, `/Lessons/Science_Teesside/`, `/Lessons/Humanities_Teesside/`** (the
builder's `LESSON_ADULT` set in Site `domain-split/education_support.py`) — MATT ruling.
`/for/pupils/`: no Ko-fi. "game/games" in nav/titles/headings: 2 site pages
(`/game-saves/`, `/privacy/`), 10 lesson pages (content words), 0 apps — reported, not
edited.

## P2 — GLITCH + LINK SWEEP · HC1_P2_OK (measured on the local mounts)

2.1 1287 education html routes + 81 play routes fetched: all 200 (local mount;
served-bytes identity is CI's, see map §3).

2.2 Static contract (education / play): one `<h1>` 633 / 44; zero 49 / 1; several
(deck-per-slide) 684 / 36. `lang` missing: 1 (`/hub-highlight-card.html`). viewport
missing: 14 (7 GROW `v3_40min` Science decks, hub-highlight-card, …). **Zoom blocked
(`user-scalable=no`/`maximum-scale=1`): 19 education** — 12 Apps studios
(`Exit_Ticket`, `Video_Studio`, `Seating_Studio`, `Feelings_Checkin`, `Rubric_Studio`,
`Quiz_Studio`, `Writing_Frames`, `Classroom_Toolkit`, `Animation_Studio`,
`Mindmap_Studio`, `Art_Studio`, `Design_Studio`, and 7 more studios; none outside Apps) —
and **7 play** (`apexrally`, `Orbital`, `fracture`, `neonsync`, `neonmeridian`,
`neonbreach`, `MedevacFrontier_v1`). Tier 1 per §5.1; HELD as proposals because the
studios draw on canvas and need real touch testing before the viewport changes (R5).
Duplicate ids in the DOM: 19 education pages (Estate_v3 Humanities tier sections
`W1Supported/W1Standard/W1Stretch`), 0 play. `rel=canonical` missing on 1338 education
lesson pages (report). Small targets (<44 px) >30 % of visible controls at 390 px: 320
education pages, 16 play. Way-out by real Tab walk: not reached within 50 tabs on 203
education routes (limitation: the adapter's nav is appended at the end of `<body>`,
after the deck's controls) and 10 play (splash gating; the Site's own
`inline-exit-verify` is green 2026-09-06 and is authoritative).

2.3 Internal links/assets: education 9 647 refs, resolver flagged 60, of which real:
- **19 decks with a dead "⌂ Start Here"** (`../../START_HERE.html` from a donor chassis
  written two directories deeper): 12 `LAUNCH_ASDAN/Autumn1_W1-W7_2026-27` → **fixed in
  PR #340 (HELD)**; 6 `Art_Teesside/LAUNCH_W1-W8_2026-27` (pack has no hub; target
  ambiguous → MATT); 1 donor.
- 5 hub `START_HERE.html` pages with 7 cross-term cards to files that do not exist at
  that path (`…W1_My_Routine…`, `…W16_Partner_Challenge…`, `GROW_ASDAN_W15/W16` live in
  `Spring1_W1-W6_2026-27` under other names; `BUILD_ASDAN/Community_Project` card href
  truncated at the apostrophe of `COMM_W2_The_Site's_Need.html`) — interpretive, MATT.
- 2 `*_Estate_v3/*/index.html` → `_glv3/*.md` (directory excluded from publication).
- `/stats/on-this-device/` (Site-generated by `usage_discovery.py`): six relative assets
  (`../styles.css`, `../favicon.svg`, `../assets/mbm-features.*`, …) resolve to
  `/stats/…` → 404s, page unstyled; pupil-reachable via `/stats/`. Tier 2, Site builder.
- `/artsaward/` 4 relative download names (generated client-side; expected absent).
- 2 primary Y4 decks: "← Lessons" is `../../../../index.html` → site root, not `/Lessons/`.
Play: 0 real (134 flagged were bare-specifier imports under import maps + template strings).

2.4 Runtime (headless Chromium, 390×844, external hosts aborted): education 38 routes
with console/page errors — 17 external-block noise, 7 CSP (1.5), **4 ASDAN decks
`TypeError: Cannot set properties of null` in `registerXP` / `pres-num`**
(`GROW_ASDAN/Autumn1_W1_2026-27/GROW_ASDAN_W1_Choose_My_Young_Duke_Challenge.html` —
**taught now** — `Autumn1_W7…W7_A_Visitor…`, `BUILD_ASDAN/Autumn2_W7_2026-27/…W15…`;
the scorebar markup `#xpCount/#xpTotal/.pres-num` is missing; navigation still works,
XP feedback dead), **12 `GROW_Estate_v3/GROW_ASDAN/{COMM,ENT}_W*.html`
`ReferenceError: taTitle is not defined`** (recorded in the sweep; not reproduced on a
plain reload — event-driven; investigate). 17 routes with failed same-origin requests:
**10 Art decks fetch `../../tools/artsaward/SLOTS.json` → 404** (`tools/` is excluded
from the education publication; pre-split it was served) — the staff slot panel falls
back to "Unconfirmed — preparation only" + manual file upload; 7 CSP-blocked assets.
Play: 0 errors, 0 failed requests. First paint (local, no throttling): education median
120 ms / p90 276 ms; play median 1.06 s / p90 5.9 s, 19 routes load > 3 s without a GPU
(`Prism` 17 s, `Slipstream` 15 s, `Orbital` 14.8 s, `Neon_Garden` 14.4 s).

2.5 Workflows (latest on main): Lessons 17 all green at HEAD except dispatch-only
`j4-absolute-ref-probe` (08-05), `wave-ohm-deck-live` (08-12), `glv3-production-byte-check`
(08-22) — the latter two are now structurally stale (raw-vs-served byte compare with the
navigation adapter injected). Reds on 09-06 at `e7dcbfb` explained: served job
INCONCLUSIVE (games publication in progress), unification live-proof read the raw menu
(fixed by #339); green at `904a3c1`. Site: **`estate-check-health` red on every run since
08-26** — its report step exits 1 by design when it finds gaps ("0 required for merge
anywhere" for Lessons/Apps, Games merged over red ×9); a finding, not a broken
instrument. **Structurally wrong after the split (fetch game routes on
`madebymatt.uk`): `published-live-verify` (schedule Mon 06:17 UTC — will red from
2026-09-07 on `/ouroboros/ /novasiege/` stubs), `driving-games-live-verify`,
`neonbreach-live-verify`, `olympics-live-verify`, `titan-crown-live-verify`,
`mtr-live-gate`, `post-merge-production-verify`, `v4-games-deployment-verify`
(canonical literals), `apexpool-home-verify`, `mbm-audience-discovery-closeout`;
`agx1-live-verify` already reads the play shelf.** Repair = retarget their origin
table to `https://www.madebymatt-play.uk` and compare against the host-rewritten
publication, not raw source. Not done this session (R3 order respected: nothing merged
that depends on them). Games 8 green; Apps 8 green.

2.6 External links: BLOCKED-ON-VENUE (proxy denies every external host). 0 measured,
0 removed.

## P3 — LESSON QUALITY (Autumn 1 = weeks 1–8, spine `_sownb/TERM_DATES.md`)
Deck set from catalogue evidence (`tools/catalogue/TERM_AND_STYLE_EVIDENCE.json` term
Aut1) ∪ `SCIENCE_WEEK_BINDINGS.json` Aut1: **402 decks**.

| pathway | checked | green | fixed | held | ledgered |
|---|---|---|---|---|---|
| BUILD | 135 | 135 | 0 | 0 | 5 zero-size svg (Science W3–W7 sprite) |
| GROW | 102 | 81 | 0 | 0 | 4 JS-error decks (2 taught now) + 12 Estate_v3 taTitle + 1 SLOTS 404 + 1 no home link (`GROW_HUM_W7`) + 5 svg |
| LAUNCH | 119 | 119 | 12 (PR #340 HELD) | 0 | 6 Art LAUNCH dead Start Here |
| OTHER (primary etc.) | 46 | 46 | 0 | 0 | 37 load Google Fonts/YouTube |

Per deck: zero console errors 382/402; every image renders (0 broken `<img>`); 10 decks
carry one zero-size `<svg>` (needs eyes; likely a sprite); guidance hidden by default
**402/402** (no `.ta-card/.mbm-guide/[data-audience=staff]` element visible before the
toggle); print path renders every deck (402 PDFs, median 4 pages, max 12); way home
present 401/402; `lesson-config` valid JSON on all 38 that carry one, 5 lack `family`
(`SCI_B_W8A/B`, `SCI_L_W8L1-3`), 5 Humanities configs whose `title` differs from the deck
(`BUILD_Humanities_W4/W5/W6`, `GROW_Humanities_W4/W5`) — possible donor leak,
interpretive; **140/402 have more than one top-level `:root{}`** (chassis + theme block;
report). Reading-level and the rest of the VB battery (g23–g30), hub catalogue and
classic-v2 contract: CI green at `904a3c1` (fieldops run 34038867832), cited not rerun.
Contrast: sampled ratios computed but background inference is unreliable on slide
decks — not reported as a defect. Content accuracy (3.3): no interpretive items raised;
only objective change is PR #340. Lesson↔game (3.4): 6 hubs link to play, 0 embeds.

## P4 — PERFORMANCE
Play bytes: 69 payloads, 20.3 MB raw / 6.3 MB gzip; **16 over 500 KB raw** (table
`p4_bytes.txt` in the session; largest `/titanforge/` 1.6 MB + 589 KB vendor,
`/apexkick/` 971 KB, `/emberwild/` 967 KB, `Wrecking_Crew` 846 KB). Enforced: nothing.
Boot (local, 390 px, no GPU): median 157 ms, p90 1.5 s across the 69 payloads.
Idle-repaint census (requestAnimationFrame calls in 600 ms of idle after load+3.5 s):
**18/69 paint zero frames at idle**; busiest title screens `/offbrand/` 148,
`Charcoal` 144, `/apexgolf/` 72, `Wrecking_Crew` 70, `/titanforge/` 52, `/hyperdraft/`
52, `/voxel/` 50 (report only; a firing control for "one input → exactly one paint" was
not built). Runner-fps gate:
no single named gate found in Site tools (per-game flash/flicker measures exist);
not run. Education LCP/CLS/TBT on a throttled profile: NOT MEASURED (local FCP only).
4.3: nothing changed.

## P5 — CORRECTIONS
- **PR #340 (Lessons, HELD)** `claude/hc1-p5-wayhome-launch-asdan` @ `d88d436e`:
  12 LAUNCH ASDAN Autumn-1 decks, dead Start Here → own hub; checksums refreshed;
  proof and firing control in the PR body. Rollback `904a3c1a`.
- Everything else: HELD as proposals (below), no branch.

## MATT-ONLY
1. R12 pair: confirm `Lesson_VIR_Pupil_App.html` and `R_Gate_Calibration_Game.html`
   stay on both domains.
2. Ko-fi footer on `/Lessons/`, `/Lessons/Science_Teesside/`, `/Lessons/Humanities_Teesside/`,
   `…/Teaching_Packs/`, David cover index — remove from `LESSON_ADULT`
   (Site `domain-split/education_support.py`) if these count as pupil surfaces.
3. Site builder: publish `tools/artsaward/SLOTS.json` (add to `public_file` like
   `tools/index.html`) then bump `builder_ref` in Lessons/Apps `education-pages.yml`.
4. Site builder `moved_page()`: add `<link rel="canonical" href="<play url>">` to stubs.
5. Verifier retargets (2.5 list) — first file
   `.github/workflows/published-live-verify.yml` (`--origin https://www.madebymatt-play.uk`).
6. `/stats/on-this-device/` asset paths (Site `usage_discovery.py`): root-anchor them.
7. Zoom-blocked pages (2.2 list): drop `user-scalable=no`, add `touch-action:
   manipulation` where drawing needs it; needs touch testing.
8. GROW ASDAN `registerXP` decks ×4: restore the `.pres-scorebar` block or null-guard.
9. Estate_v3 GROW ASDAN `taTitle` ×12; six GROW Humanities CSP decks; hub cross-term
   cards ×5; Art LAUNCH Start Here ×6; primary Y4 "← Lessons" ×2; Humanities config
   titles ×5.
10. Not measurable here: http→https redirect and `www.` on education; external links.

## RESUME BLOCK
Next: P5 verifier retargets (R3) in Site, then items 3–4 (builder), then Tier 1 decks.
First file: `mattroper1977.github.io/.github/workflows/published-live-verify.yml`.
SHAs: Lessons `904a3c1a` (PR #340 head `d88d436e`), Site `d9115711`, Games `ec5ee7bb`,
Apps `96d160f0`; builder pin `ff764b65`; Lessons pins `5ba6271` (Site/Apps builds),
`78ff8fc9` (play).
