# ORDER LV2–LV6 record, and corrections to the FIN1 close-out — 18 September 2026

**Governs:** ORDER LV2 (refutation pass and five landings), LV3, LV4, LV5 and LV6.
Written under LV6 part X1, as a docs-only pull request.

**Purpose.** `docs/orders/FIN1_CLOSE_2026-09-17.md` is the FIN1 record. Parts of it are now
known to be wrong or incomplete, and LV2–LV6 measured things it could not. This document
corrects that record where it is wrong, closes the open items that are closed, and states
what is parked and why.

Nothing here is a new ruling. Where a thing is unmeasured it says so.

---

## Corrections to the FIN1 close-out

### 1. `#571` is reverted. It is not in the estate.

The close-out's estate table reads *"Lessons | `1cd2ef2` (`#571`) | … 26+1 stamped Games
routes (`#571`)"*. That is no longer true and, at the moment it was written, described a
change that had landed **without the reviewed source revisions that admit it**.

- `#571` stamped 27 routes and merged.
- It broke the Lessons publication. Measured green→red at exactly `1cd2ef2`: publication
  runs `35252654077` (success) → `35255248965` (failure) → `35275580333` (failure) →
  `35281981947` (success, after the revert).
- `#573` reverted it as `5a560d5`.
- It has **not** been re-landed. Re-landing it is what LV6 part C2 attempted; C2 is parked
  (below).

The error condition was also mis-named at the time. `domain-split/play/source_revisions.py`
raised one message for two different states. Measured across the 29 stamped checkouts:
**9 matched exactly one reviewed revision, 20 matched zero, 0 matched more than one.** The
condition was UNREVIEWED, not ambiguous. Site `#403` split the message so the two states
can never again be read as one.

### 2. The R1–R7 widening in `#570` was narrower than the gate it replaced.

`#570` is recorded as making the estate stricter. On population closure it did the
opposite. Proved by running the pre-widening gate body against the identical tree: the old
body reds on Lessons main with *"makerSplash.applied is not the exact ordered R1-R7
population"* where the widened body is green. Correct stamping is necessary and is **not**
sufficient.

`#574` restored that authority. `tools/verify_v6fin_w7_r1_r7.py` now carries

```python
AUTHORISED_BEYOND_R1_R7 = ()
```

and refuses any route declared outside `R1–R7 + AUTHORISED_BEYOND_R1_R7`. The population is
R1–R7 and nothing else; a route joins it only by a reviewed release that also extends that
tuple.

One control written for this was itself vacuous and is recorded as such: the first M7 fired
on *"is both applied and excepted"*, not on the closure rule it claimed to test. It was
re-run as M7b with an unstamped, undeclared route, which is the rule's own case.

### 3. The D and E figures are local-run, not CI measurements.

The close-out states this for D's browser run and not for the rest. Stated plainly here:
**every figure in the D and E tables was measured in the agent container with bundled
Chromium**, not by CI and not against the live origin. The container cannot reach
`madebymatt.uk` or `madebymatt-play.uk` — the proxy denies CONNECT — so no figure in those
tables is a served measurement. What CI did measure is named where it is named: the 44px
target failure (`390-targets-44px-closed-drawer-sheet`, the 208×32 box) was found by CI, not
here.

The substance of D and E is unaffected. Their provenance was overstated.

### 4. The witness blind spot was structural, not a one-off.

The close-out records cancelling a duplicate publication run as *"one mistake of mine"*. It
was also a defect in the witness, and LV3 part W fixed it:

- `exit_code()` now separates the three outcomes rather than collapsing two of them:

  ```python
  def exit_code(report):
      verdicts = [r['verdict'] for r in report['repositories'].values()]
      if 'RED' in verdicts: return 1
      if 'INCONCLUSIVE' in verdicts: return 2
      return 0
  ```

  Before this, a run exiting 0 with 2 of 4 repositories INCONCLUSIVE read as green. I
  reported one such run as green in this session and corrected it in the same turn; that
  misreading is what W removes.
- Run selection no longer requires the first matching publication run. A non-success match
  is skipped with a named reason and the search continues, so one cancelled run can no
  longer blind the witness for a commit.

  **Correction, 18 September 2026 — this is true of ONE of the two copies.** W made the
  *Site* selector conclusion-aware: `tools/lib/publication_artifacts.py`, which is what
  `tools/hc5/serve_witness.py` imports and calls. The Lessons estate carries a second,
  unshared copy of the same tool — `tools/prepare_served_publications.py`, ten shared
  function names, `prepare_one` divergent — which still hard-stops on the first
  head-matching run regardless of conclusion:

  ```python
  require(run.get('conclusion') == 'success',
          f'{kind}: exact-source publication {run["id"]} failed ({run.get("conclusion")})')
  ```

  It is live: `.github/workflows/fieldops-p2-and-sweep.yml` runs it for the serve proof,
  and it is digest-pinned in `tools/verify_cross_estate_unification.py` and in
  `tools/catalogue/pin_catalogue_contract.py`. It is safe today only because
  `education-pages.yml` carries no `concurrency:` block — verified, zero matches — so
  nothing cancels the runs it selects from. Nothing compares the two copies, which is the
  same undetectable-divergence hazard the estate gate copies are guarded against and this
  pair is not. Unifying them, and the comparison control that would have caught the
  divergence, go to the next order with the fallback limb and STOP-P1. **FINDING, NOT A
  FIX — deliberately not changed inside this release.**

### 5. Ruling F is now a named allowance, not an open item.

Close-out open item 4 said the witness *"needs one ruling, not one fix"*. LV3 part B applied
it. `tools/hc5/serve_witness.py` gained `_RecordHops`, `www_label_only(url, final)` and
`PERMANENT_REDIRECTS = (301, 308)`, and allows a redirect only when all three legs hold:

```python
legs = {'wwwLabelOnly': www_label_only(url, final),
        'singlePermanentHop': len(hops) == 1 and hops[0][0] in PERMANENT_REDIRECTS,
        'bytesIdentical': served == expected}
```

The measurement that justified it: `landed=https://www.madebymatt-play.uk/`, one permanent
hop, apex → `www` label only, bytes identical. Anything else is still red.

### 6. Close-out open items 1 and 3 are closed.

- **Item 1, the Apps half of the catalogue digest pin.** Landed as Apps `#106`, `#107`,
  `#108`. Verified on the real trees today: `pin_catalogue_contract.py --check` returns
  `"status": "PASS"`, `reviewedFiles: 500`,
  `gateSha256: ae82d747d2020e4c2ec02b345cf1090ae11646ff3e81c3095c7b98fc1ed4097d`, with both
  copies of `tools/verify_cross_estate_unification.py` carrying the same
  `tools/verify_v6fin_w7_r1_r7.py` pin `f864bce7…`.
- **Item 3, the Lessons-owned rows of `docs/play-q1/coverage.json`.** Gated by LV3 part D:
  `splash-region-records.yml` gained a `records-current-lessons` job that runs
  `play_splash_coverage.py --owner lessons`, one step behind, so neither estate reddens for
  a change it cannot fix. `--owner` was generalised to `all | site | lessons` for this.

---

## Close-out open item 2 — re-pinning the canonical splash generator: measured and PARKED

This is the item the close-out called *"a release, and it needs its own pass"*. That pass is
LV6 parts C1 and C2. C1 reviewed 35 routes and Matt ruled on each: 34 approved, `Games/Voxel_Frontier.html` not. C2 is **parked** on a
blocker that no amount of care removes, because clearing it requires a review record only
Matt writes.

**What the re-pin is.** `.github/workflows/v6fin-w7-r1-r7.yml` pins the canonical generator
at Site `acd9f0cc`, in two places (lines 61 and 110). This is the only live pin of it; the
Site mentions are historical prose. Moving it to Site main `76ab714` re-cuts the generated
region on every route that already carries one. Lessons `main` declares **7** applied routes
(R1-R7); C1's approved set is **34** — those 7 re-cut, plus 27 newly declared.

**The approved tree meets the generator criterion.** Built and measured:
`render_maker_splash.py --check` reports **`34 applied target(s); 0 written; 0 divergent`**.

**The blocker is the lesson order projection, and it lands on R1, R3 and R7.** On that same
34-route tree, `tools/catalogue/build_lesson_order.py` returns

```
unresolvedTiming: ["5 Intervention 10/Lesson_VIR_Pupil_App.html",
                   "5 Intervention 10/InterventionA_Battle_Arena (1).html",
                   "5 Intervention 10/InterventionB_Escape_Room.html"]
```

so `pin_catalogue_contract.py` refuses with *"Part L lesson order differs from its accepted
evidence projection"*. Those three are **R7, R1 and R3** — the R1-R7 contract's own subjects.

**Why those three and no others.** They are the only routes in the set carrying an accepted
`sha256` in `tools/catalogue/TERM_AND_STYLE_EVIDENCE.json` that Lessons `main` matches
byte-for-byte:

| route | accepted `sha256` | main | after re-cut |
|---|---|---|---|
| R7 `5 Intervention 10/Lesson_VIR_Pupil_App.html` | `d8b38bbe…` | matches | `98d10dc9…` |
| R1 `5 Intervention 10/InterventionA_Battle_Arena (1).html` | `0c8d2594…` | matches | `326b849e…` |
| R3 `5 Intervention 10/InterventionB_Escape_Room.html` | `f84ff257…` | matches | `11f5432d…` |

While the bytes match, `build_lesson_order.py` never asks them for proof — the whole
`digest != e['sha256']` block is skipped. The re-cut breaks the match, the block runs, and
**every** proof branch is False for all three:

```python
proved = enrichment or explicit_cell or preserved_outcome \
         or (proofs and all(norm(p['quote']) in text for p in proofs)) \
         or (approved_science.get(path) == digest and timing and all(norm(q) in text for q in timing))
```

Measured for all three after the re-cut: `enrichment` False, `explicit_cell` False,
`preserved_outcome` False, own-title-slide `proofs` **0**, and not present in
`tools/easter/SCIENCE_ORIGINAL_TARGETS.json` at all, so `approved_science.get(path)` is
`None`. Their only evidence method is `current presentation structure`, which carries no
re-provable quote: the record's whole force is the byte pin itself.

**They cannot be parked out of the way, because they are the contract.**
`tools/verify_v6fin_w7_r1_r7.py` requires the declaration to open with the exact ordered
R1-R7 population, so dropping them from it is refused outright. Holding them at their old
bytes instead was measured and gives **`0 written; 3 divergent`** — with the projection back
to `unresolvedTiming: []`, which is the control that isolates the cause to exactly these three
and nothing else.


The only remaining way through is a newly accepted `sha256` for those three routes. That is
a review record. ORDER LV4: *"Do NOT manufacture review records. Never write an approval
field or a `published_sha256` on my behalf; those are written only after I approve a named
route at a STOP."* So C2 parks, per LV6: *"Any criterion unmet → park, report, do not
loosen."*

**A second, independent blocker on the same part.** Five of C1's 34 approved routes are
baseline-only.
They need a rollback revision to survive the merge gap — the Site publication pins Lessons at
`b54c9006`, where their bytes are still the old ones — and writing that revision requires an
`approval` field, which is the same forbidden record.

**What is needed to unpark C2:** one ruling accepting new `TERM_AND_STYLE_EVIDENCE.json`
digests for the three routes above, and one sentence to serve as the five baseline-only
routes' rollback revision record (or an instruction to omit the `approval` field). Nothing
else in C2 is blocked, and nothing needs preserving to resume it: the re-cut is deterministic.
Move the two generator pins in `.github/workflows/v6fin-w7-r1-r7.yml` from `acd9f0cc` to
`76ab714` and run `tools/render_maker_splash.py --root .` from that Site checkout, and the
same 34 routes come back byte-for-byte. The C2 working tree built in this session lives only
in an ephemeral container and is deliberately not pushed: it carries stamped bytes that have
no reviewed revision behind them, which is the whole reason C2 parked.

---

## `Games/Voxel_Frontier.html` — what the education publication actually serves

C1 ruled this route **not approved**, and it was used as the population-red control. It was
also recorded as absent from `games.json` (62 entries), `preservation.json` (69 payloads) and
`source-revisions.json`. Measured today, that absence is irrelevant to what is served.

`domain-split/build_education.py` diverts the route twice over, by prefix and by classifier:

- the copy loop takes the stub branch on `name == 'lessons' and relative.startswith('Games/')`;
- `education_policy.classifier`'s `is_game()` returns True on `path.startswith('/Lessons/Games/')`.

Neither consults a manifest. So the Lessons education publication **does** serve
`/Lessons/Games/Voxel_Frontier.html` — as a generated *"This game has moved"* stub whose
canonical link is `https://www.madebymatt-play.uk/voxel/`. It never serves the game's own
bytes. A splash region stamped on that file would not be seen on the education site at all.

---

## Census and mutation tests of the gates landed in Site `#395`–`#398` and Games `#92`–`#97`

Read-only census first. Games `#95` was never merged; `#94`, `#96` and `#97` move
`play-publication.json` only. The gate, check, verifier and witness changes are:

| change | landed in | mutation | result |
|---|---|---|---|
| `tools/check_splash_routes_lf.py` | Site `#396` | CRLF one stamped route | RED, 3869 CR bytes named |
| `tools/check_play_q1_readme_digests.py` | Site `#396` | plant a hex digest / plant a byte count | RED on each rule separately, line and token named |
| `tools/play_splash_coverage.py --owner site --check` | Site `#396` | one space inside a stamped region | RED, committed vs writer digest named |
| `tools/render_maker_splash.py --check` | Site `#395` | same | `1 divergent` |
| `tools/cyberpulse/verify.mjs` `SPLASH_BYTES` | Site `#395` | one space inside CyberPulse's region | `FAIL CP6s — 20560 bytes (estate pin 20559)` |
| `tools/verify_maker_splash.mjs` GD1/WO1 | Site `#395` | restore the pre-fix defect (let `primary()` resolve to the way out) | `FAIL WO1 … the region's hand-off never lands on the exit`, `handedTo: "mbmexit-back"` |
| `tools/site_build_scope.sh` | Games `#92`, `#93` | a builder whose `--help` fails | `rc=1`, fatal — not read as "predates the flag" |

**Each fires on its own rule, and only its own rule.** Proved two ways round rather than
assumed:

- a region byte change reds the generator and the coverage gate, and leaves
  `check_splash_routes_lf.py` **green**;
- a CRLF rewrite of the same file reds `check_splash_routes_lf.py`, and leaves the generator
  (`0 divergent`) and the coverage gate (exit 0) **green**.

So the LF check is not redundant with the digest gates: it catches exactly what they read in
text mode and cannot see. `render_maker_splash.py` and `play_splash_coverage.py` both use
`Path.read_text(...)` with no `newline=` argument, which is universal-newlines mode.

`site_build_scope.sh`'s `run_logged` was exercised on both of its rules: with a Python parent
shelling out to a failing child it surfaces the **child's** `ValueError`, not
`subprocess.CalledProcessError`; with only a `CalledProcessError` to report, it reports that
rather than dropping the failure.

`domain-split/play/prepare.py` (Site `#397` branch commit, merged as `#398`) is a
build-scoping change, not a gate, and was verified by reading:
`build_publications.py:385` guards the education front doors with
`if args.publication in ('both', 'education')`, and the flag defaults to `both`, so existing
callers are unchanged. It is exactly the capability `site_build_scope` probes for.

Run locally with bundled Chromium; the browser verifier control was `controls: 81/81` before
the mutation and `80/81` after, with the three RL4 red-proof controls green throughout. The
mutation provoked the hand-off half of the way-out fix; the *"a Tab pressed on the exit
leaves the exit"* assertion stayed green because the `armWayOut` same-element guard was left
in place. Both halves are asserted separately by the gate, and only one was provoked here.

---

## Still open

1. **C2, parked.** Needs the two rulings named above.
2. **The stamped Games routes.** Still reverted, so close-out open item 5 stands in a
   different form: there is nothing merged-but-unserved to admit, because the stamp is not
   merged.
3. **`docs/play-q1/coverage.json` Lessons rows** are gated, but they are gated one step
   behind by design. They are current only after the writer is re-run following a Lessons
   change.

## Limits observed

No repository, Actions or ruleset setting was changed. No force-push. No branch deleted. No
pull request closed. No check, gate, control, threshold, scope or population was disabled,
skipped, excluded or loosened — where a criterion could not be met, the work parked instead.
No approval field and no `published_sha256` was written. The registry, `evidence.json` and
`preservation.json` were not touched. Every mutation above was applied to a working tree,
measured, and reverted; all four trees are clean.
