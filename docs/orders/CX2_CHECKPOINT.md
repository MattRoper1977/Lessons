# CX2 checkpoint — 15 September 2026

Append-only. Every entry: pass · source identities · owned files · PR/head · run id · exact next action.

## 0. First actions
- 0.4(a) Order committed verbatim: `docs/orders/CX2_FINISH_2026-09-15.md`.
- R3 done: `codex/edu-q1-grow-w3a-focus-20260915` fast-forwarded 1fa3ab94712bef3320c625000de9e978f8cf3d7c → 8d155f299c53e133d525d4fcc64bbc733b5d3250 (ancestry verified, tree 0e4c070a1114cb585446cc40c12c43c32f2a9861 = the tree the 120-stage acceptance and the 430-control GLV3 self-test ran on, so 2.1 cites those runs). PR #538 head now 8d155f2 (6 commits, 58 files). Branch `claude/made-by-matt-handoff-ycul48` deleted on the remote.
- Next: 0.4(c) publisher admission of docs/, 0.4(d) policy documents, R1 record, R5 diagnosis, R4 mechanism search.

## 1. First actions completed — 15 September 2026
- 0.4(c) `docs/` is in the Site publisher's `SKIP` set (`domain-split/build_education.py` at Site main c63149ead07c33e35d78bbdc8935b7934e7d292b: `public_file('docs/orders/CX2_FINISH_2026-09-15.md')` → False). Docs are not served.
- 0.4(d) Policy documents, supplied by Matt in this session and hash-recorded (not committed; uploads only):
  - Progress Schools Curriculum Policy 2026/2027, Issue 2 (August 2026): `PS_Curriculum_Policy_-_2026_2027.pdf`, 444,764 bytes, SHA-256 `593ab59361d1f1d22360a0ba12bdb96e08dcd94404a07368c09768414e982aed`, 41 pages. Confirms §14.3 minimal codes VF WS I NS E R, §14.4 Lundy, Tier 2/3 vocabulary, three-year thematic cycle, "digital software app" wording, Boxall as placement/progress instrument.
  - Feedback & Marking Policy 2025/2026 (Pilot), Issue 1, May 2026, EFL wording, 16 pages: `Feedback_Policy_2025-2026_-_Pilot.pdf`, 208,449 bytes, SHA-256 `41046284a00c9218679b946134cbc6e53c1897aad28b15fc4bd4aa963720ff3b` (the master-order reference copy; also on Drive as 1D8lpR-_1uQc2ND5C02T7BUfM8myBrC6M).
  - Second copy, same policy, Issue 1, Earwig wording, 13 pages: `Feedback_Policy_-_2025_2026_Pilot.pdf`, 204,058 bytes, SHA-256 `aa41d67963846cf35e9bb6e53e708c7b46725c2d5cef432c2e1994a78e434e53` (Drive 15hGPn0lGji4WRt-A_L2_19nidpP6XMRb). Both are Issue 1.
  - Feedback & Marking Policy Issue 2: searched this session's uploads, the Lessons repository (all history), Drive titles and full text. Not found. R9: Issue 1 remains the code source; S6 does not arise.
  - `13-Gathering_Feedback_and_Portfolio_Evidence.pptx` (SHA-256 `04dbc4f3b08d99744b12a2a84a3965bbc3dcc866f8522d16946db4bb59b7cb55`) is an Arts Award lesson deck, not a policy.
- R5: `check_catalogue_static.py` red on main 929cf731 (51 Spring/Summer Science lesson rows absent from TERM_AND_STYLE_EVIDENCE.json). Own PR #539 from branch `claude/cx2-r5-catalogue-evidence` head d10543f2d41c3e47c98f9f27d2a82153a7396ae3: generators re-run, red-before/green-after, planted control red, pins re-cut. Next: required CI green → merge → publication settled → rebase #538.
- R4 finding: `Teaching_Packs/GROW/HTML/*.html`, `Resources/*.html`, `Teacher_Notes.html`, `Pupil_Resources.html`, `GROW/index.html` are not exports of the canonical lesson (different chassis, no slide elements, one script, no long byte runs shared with the 6 September source). No generator exists in the repository or its history (`git log -S mbm-offline-classroom` finds nothing); they arrived whole in PR #431. Sugar's six native companions were produced outside the repository; the in-repo mechanism for Friction's DOCX/PDF is `tools/grow_resources/author_w3*.py`. STOP S3 raised in the readback with options; other lanes continue.

## 2. §2.3 reading-band report (Friction, report only) and S3 — 15 September 2026
Instrument: `_sownb/vb/tools/g26_reading_band.py` v1.3.0 (13/13 controls fired), bands from `_sownb/STYLE_CONTRACT.json` (GROW 3.0–7.0, BUILD 1.0–4.0, LAUNCH ≤14.21 ceiling), Flesch-Kincaid on pupil-addressee text.

| Text | Pupil FK | GROW band |
|---|---|---|
| Whole lesson (instrument's own run) | 6.98 | PASS |
| Arrival Supported / Standard / Stretch | 5.64 / 5.76 / 5.44 | in band |
| Lesson A exit Supported / Standard / Stretch | 3.70 / 6.54 / 5.88 | in band |
| Knowledge organiser | 7.51 | above |
| Review the evidence (B) | 6.96 | in band |
| Help prompts | 5.19 | in band |
| Lesson B exit slide (unchanged legacy) | 7.87 | above |
| Print scaffold Supported / Standard / Stretch | 7.80 / 5.62 / 5.29 | Supported above |
| Print worksheet Supported / Standard / Stretch (legacy) | 7.33 / 7.96 / 7.70 | above |

Accepted lesson: no re-open (4.2). Matt list: organiser, W3B exit, Supported scaffold and the three legacy worksheets read above the GROW band. Sugar (accepted): pupil FK 5.26 against BUILD 1.0–4.0, RED report-only, Matt list.

**STOP S3 (R4).** The stale offline companions (`Teaching_Packs/GROW/HTML/*.html`, `Resources/*.html`, `Teacher_Notes.html`, `Pupil_Resources.html`, `GROW/index.html`) are linked from the public hub `Science_Teesside/Teaching_Packs/web-slides.html` and listed as retained alternatives in `tools/downloads/SOURCE_PLACEMENT.json`. They are a separately authored offline chassis, not an export of the canonical bytes; no generator exists in the repository or its history. Options: (A) author a new in-repo exporter that regenerates the W3A/W3B offline companions from the current canonical lesson in the same chassis — new design work, needs a ruling; (B) inside #538, retire the two Week 3 companion entries on web-slides.html/GROW/index.html/Teacher_Notes.html to a clearly labelled superseded section that links the canonical lesson and the native pack (satisfies §25.4, smallest change, no generator needed because nothing is regenerated); (C) leave as is and ship the duplicate (violates R4). Friction Ready waits on this ruling; every other lane continues.

## 3. R5 PR #539 — CI failure fixed — 15 September 2026
- #539 head d10543f failed `Catalogue schema, tags and contract` at `companion_catalogue.py --self-test` ("evidence entry differs from its derivation"): `build_catalogue.py` had overwritten the 116 companion-pack evidence entries that `tools/ux2/companion_catalogue.py --write` owns.
- Fix commit 28610814313d0ca6deba8e6f45085e247f9370ed on `claude/cx2-r5-catalogue-evidence`: `companion_catalogue.py --write` re-run after the rebuild (116 entries re-derived, 0 pack rows changed), `build_lesson_order.py` re-derived, static check re-run, the two derived-file pins re-cut in `tools/verify_cross_estate_unification.py` (apps.json digest restored from origin/main, not a stand-in). `resources.json` unchanged (951 rows).
- Owned files in the fix: `assets/catalogue/lesson-order.json`, `tools/catalogue/STATIC_CHECK_RESULTS.json`, `tools/catalogue/TERM_AND_STYLE_EVIDENCE.json`, `tools/verify_cross_estate_unification.py`.
- Local proof on the fix tree (each exit 0): `build_display_titles.py --check`, `check_display_titles.cjs`, `check_catalogue_schema.py --self-test` (8/8 plants red) and plain, `unit_tags.py --self-test`/`--check`, `prove_catalogue_gate.py` (8 controls), `companion_catalogue.py --self-test` (6/6 plants red)/`--check` (116 packs), `build_spine.py --check`, `resource_sizes.py --check` (1463 sizes), `check_catalogue_static.py`, `build_lesson_order.py --check` (1075 entries), `derive_triggers.py --check` (471 asserted, 477 triggers).
- Evidence delta versus main 929cf73: 51 Spring/Summer Science entries added, 46 Science/Humanities entries with a refreshed `sha256` (8 with refreshed evidence quotes), 0 removed; the 116 pack entries equal their derivation.
- CI on 2861081 in progress (ux2-gates run 35008854934, S1L/static-contract run 35008854986). Session subscribed to #539 events. Next: required checks green → merge (one merge; Pages deploy settled) → rebase #538 → re-run pack/binding/size/lesson-order/PIN1/GLV3.

## 4. §4.1 standard written; §4.6/§4.7 measured; #539 browser-matrix re-run — 15 September 2026
- §4.1: `docs/orders/LESSON_STANDARD_2026-27.md` written (master §25.1–25.5 verbatim base, each line annotated with the corroborating Curriculum Policy Issue 2 clause; B1 reading bands from `STYLE_CONTRACT.json` row `reading.pathway.band` at 40f60d78841e95855aa345e26482dae6d7fd7a32; B2 feedback card with the Issue 1 §11 Code/Meaning table and R10 `E` wording; B3 response labels; B4 Gatsby 4 staff link; B5/B6 findings; B7 hygiene; Part C preservation; Part D acceptance). Docs file, not served.
- §4.6 thematic cycle (measured): policy names Year A/B/C = Self and Belonging / Community and Contribution / Future and Adulthood and Autumn Transitions / Spring Growth / Summer Change. Estate: 0 files carry the three year names; the SoW records carry term themes Identity & Belonging / Resilience & Change / Our World, My Future (`_next6/sow/BUILD.json` 20/20/20, `GROW.json` 20/20/20, `LAUNCH.json` 32/32/32 + 15 blank; `_passsg/SOW_MATRIX.md`); "Year A" only as a planner title (`Build/_Archive_VersionA_LivingIndependently/BUILD_Slot_Planner_2026-27_vA.xlsx`); `resources.json` has no theme field on 951 rows. No tag invented; year letter → Matt list.
- §4.7 ELC gap row: scheme level names Pearson Edexcel ELC Science 8939 (not AQA) — `_next6/sow/slice_Science_Teesside__Grow__W8-W13_2026-27.md`, `quality/QUALIFICATION_CLAIMS_REGISTRY.json` Q-004; lesson-to-outcome mapping ABSENT (no component/outcome code in the estate; `_glv3/grow/GROW_COMPLETION_AUDIT.md`: "Science v3 does not self-award Entry Level outcomes"). Not started; AQA-vs-Pearson → Matt list.
- #539 CI on 2861081: every job green except `browser-matrix` (workflow `mbm-cross-estate-unification.yml`, run 35008854986, job 104515420369), failing in the step "HUD back and chosen-homepage controls on every Lessons game": 3 of 791 assertions — a 5 s click timeout on `5_6 Local Choice/Trekkers_Trail_Runner (2).html` and two `page.goto ... interrupted by another navigation` on `Games/Glitch_Clash.html` and `Games/Grapple.html` at 1440 px. Games code the diff does not touch; timing errors. PR `mergeable_state` = `unstable` (the check is not required). One re-run of the failed jobs requested (the single permitted re-run). Outcome recorded in the next entry.
- Egress finding (0.3): `https://madebymatt.uk` is denied by the container proxy (CONNECT 403; WebFetch EGRESS_BLOCKED). Served proof from this session runs only through GitHub Actions (`glv3-production-byte-check.yml`, `workflow_dispatch`, fixed sample list) — recorded as UNMEASURED from the container, never passed.
- Lane D measured (P5.2): `claude/rw1-w8-return-week` head b4016aca86d0e7db3638a97be2b96518784cd9e5, merge-base 509c61debf559d49aa55b5ccbba29daedd6e7c74; all seven W8 files are built there (BUILD A/B, GROW A/B, LAUNCH L1–L3; commit da5837f: pupil week tokens 0/7, render 7 PASS, timers 40.0 ×7, 27/27 links, 13 staff cards fit A4), so the order's "GROW and LAUNCH nothing built" observation is stale. Since the merge-base, main replaced BUILD W8A with the accepted Sugar lesson (5 commits) — the branch's W8A is superseded and will not be carried; W8B + GROW A/B + LAUNCH L1–L3 + `tools/rw1/` are the carry set. Its §M merge gate ("no card provenance reads asserted") can open: both Feedback Policy Issue 1 copies were hash-verified in this session (§1). The cards still carry a product name in `E` and in the provenance line → must change under R10/§4.3 before any PR. Return-week pack sources are not in the repository (only `Planning/*/…Week08_19Oct.xlsx`), so the pipeline cannot be re-run; the built bytes are carried and post-edited by a committed, idempotent tool.

## 5. #539 merged; publication blocked by the Site admission registry; carrier prepared; Lane D edits green — 15 September 2026
- #539 browser-matrix failure did not reproduce on the single permitted re-run (job 104519652069 success 19:02:01); the three failures were timing errors in Games HUD checks the diff does not touch. Merged: main `3a14e9c4d57832fed87ed588ef6f0ae4fd3d6064` (merge commit, tree 6c41207aa66c86360969dae939213ca14f79b27c).
- **Publication run 35011353286 on that merge FAILED** at the Site publisher's admission: `CHANGED education-lessons/assets/catalogue/lesson-order.json` and `CHANGED education-lessons/assets/catalogue/terms-and-styles.json`. The Lessons caller pins the immutable publisher `c06526c5578b65ac9e7524dfaa3285aeb4b1694a` (Site draft PR #381, branch `codex/edu-q1-sugar-a11y-publisher-20260915`, not on Site main); its registry admits those two derived files only at their Sugar-era digests. Merged is not served: the served tree is still the Sugar publication (run 34984264847). Every later Lessons merge stays unpublished until a new carrier is referenced.
- R6 carrier prepared. Lessons #539's two derived files, published by the pinned publisher run locally on the merged tree (`build_education.py --lessons <main tree> --allow-sparse`), measure `0091727dd9299f4788b30490bc424c863c0b379db651df59ec39dbb96d9cc0a8` (lesson-order.json) and `233bec3ca909590ed8ea4b39a643739b2d6e236f25c9478d81d9e4a88ef2b524` (terms-and-styles.json). Control: the same local run on the previously published tree 929cf731 reproduces the admitted digests exactly (lesson-order f91853cf…, terms-and-styles 6439ac05…, resources.json 2b246b0b…; HTML differs only because the sparse run has no Apps source). Old registry → carrier registry delta on the education-lessons census: exactly `{lesson-order.json, terms-and-styles.json}`, nothing else. Carrier commit `297ed5dea71461d25365fa8a9ef1139b7d161996` on local branch `claude/cx2-r5-catalogue-publisher` (one file: the registry; pairs become [published rollback, candidate]; one reviewSources note). The branch was created on the Site repository at c06526c5 through the GitHub API.
- **BLOCKER B1 (tooling, not §11):** pushing that commit to the Site repository was refused twice by the session's command policy ("CI Bypass", then "Modify Shared Resources"); the API alternative cannot carry a 635,147-byte registry. Not retried. Needs Matt: allow `git push` to `MattRoper1977/mattroper1977.github.io` for branch `claude/cx2-r5-catalogue-publisher` (or push the supplied patch). Until then §2.4 (Friction), §8.3 served proofs and every publication are held at this step. Next after the push: Lessons PR moving `education-pages.yml` (`uses@` and `builder_ref`) to the carrier commit → merge → publication run green → served proof via `glv3-production-byte-check` → rebase #538.
- Lane D (branch `claude/cx2-lane-d` from main, worktree): carry set = `tools/rw1/` (12 files) + `_sx2/{RW1_VERIFY_QUEUE,GW1A_RECORD,LW1_RECORD}.md` + the six W8 lessons (BUILD W8B, GROW A/B, LAUNCH L1–L3) from `claude/rw1-w8-return-week` b4016ac; BUILD W8A stays Sugar. New `tools/rw1/cx2_lane_d.py` (idempotent, `--check`): R10 `E` wording and verified provenance line (full policy SHA) on all six cards, §14.3 summary line, VF-led BUILD/GROW and written-for-extended-writing LAUNCH lines, §4.4 response-mode labels on exit slides, exit prints and arrival prints, §4.5 staff link in each TA brief, and the six dated pupil exit tickets on BUILD W8B (`… • Week of 19 October` in the SVG) cleared. Instrument fix: `pupil_tokens.py` now matches `Week of` and a day-month date without a year (red proof: 6 pupil hits on the unmodified W8B; 0 after). Gates on the edited set: census 0/6 pupil dates; furniture parity holds; render 6/6 PASS; every staff card fits A4 (tightest BUILD W8B marking card by 35 px).
- §4.2 gate on BUILD W8B: 5.18 → **3.26** whole lesson after `tools/rw1/edits/W8B_plain_language.json` (80 exact pairs through `tools/rw1/pupil_text_edits.py`; subject vocabulary kept; slide and print copies moved together); routes Supported 3.57 / Standard 3.76 / Stretch 3.48, all inside BUILD 1.0–4.0. GROW A/B 4.83/5.64 and LAUNCH L1–L3 7.48/7.09/6.99 are inside their bands unchanged.

## 6. Carrier pushed with Matt's authorisation; pin-move PR; Lane D PRs opened — 15 September 2026
- Matt (20:21, phone): "Authorised: push claude/cx2-r5-catalogue-publisher to mattroper1977.github.io (not main), then move the Lessons pin." Blocker B1 closed.
- Site: branch `claude/cx2-r5-catalogue-publisher` at `297ed5dea71461d25365fa8a9ef1139b7d161996` (one file, the admission registry); draft PR #382 (base `codex/edu-q1-sugar-a11y-publisher-20260915`, never to be merged, as #381).
- Lessons PR #540 `claude/cx2-r5-publisher-pin` head `dd4026251c834d56c9b95439337950be340d644d`: `education-pages.yml` `uses@`/`builder_ref` → 297ed5de; `tools/verify_cross_estate_unification.py` `PUBLICATION_CALLER_SHA256` → `05b07056346fa5dd5809e66f16c6d0181762a1cacba7b619dd4e76f22485aadb`. First revision failed `static-contract` twice over: the caller digest pin (now moved) and the standalone/offline boundary rejecting an edit to `glv3-production-byte-check.yml` (withdrawn; the served-proof instrument stays the fixed sample plus the estate's S1L deployment readback). Local gate run: PASS, positive control 3 errors. CI on dd40262 pending.
- Lane D staged as five stacked draft PRs from main 3a14e9c: #541 tools (`claude/cx2-lane-d-tools` d4a76f9, 12 files), #542 foundation (`…-foundation` d2e071d, 8 files), #543 BUILD (`…-build` b281994, 8 files; W8B `022157abe904045f782821936f0e639843cb7459152059b6c4ab0131a8cd15da`), #544 GROW (`…-grow` cc5ab85, 9 files), #545 LAUNCH (`…-launch` 261a62f, 10 files). Each pathway's admission is its own GLV3 transaction (`Return week W8 BUILD/GROW/LAUNCH`); boundary self-tests PASS with 132/158/195 controls; PIN1 472→477 asserted; static check exit 0 on each. Merges wait on #540's publication settling and land one at a time in that order.
- Still owed for Lane D: the per-pathway catalogue step (titles in `resources.json` rows unchanged; evidence/lesson-order carried in the content PRs), the companion packs (no committed generator for the W8 companions; assessed after the content lands), served proof per pathway, and the cards table for the readback (eleven staff cards on the carry set: six marking, five first-lesson-back; the order's "nine" could not be derived from any record and is reported as measured).

## 7. #540 merged and published; byte-check instrument found invalid; #538 merged with main and its boundary red fixed; Diffusion harness PASS — 15 September 2026
- #540 CI green on dd40262; merged: main `6c49a27ee51928c2c970c460e943117e5e1bf962`. Publication run 35015200068 (build + deploy) succeeded; `deploy-pages` at 19:48:18Z. Merged is not served until measured: the served bytes of the two admitted catalogue files are UNMEASURED from this session (egress denied, §4 entry). Witness URL for Matt's phone: `https://madebymatt.uk/Lessons/assets/catalogue/lesson-order.json` (expected SHA-256 `0091727dd9299f4788b30490bc424c863c0b379db651df59ec39dbb96d9cc0a8`).
- **Instrument finding (0.3, §10):** `glv3-production-byte-check.yml` run 35015846908 FAILED on every HTML row of its fixed sample (each served page is +177 bytes over the repository file) and on `resources.json` (the publisher serves the filtered manifest). The published navigation injection (NAV-2) means the instrument has compared unlike bytes since that change; it cannot pass on any current publication and its red is not evidence about this publication. Served proof remains UNMEASURED until an instrument compares the publisher's OUTPUT bytes with the served bytes. Matt list.
- #538: main merged in (`cf586170486699e501f89e5a613be376b5a198d4`; conflicts on `TERM_AND_STYLE_EVIDENCE.json`, `lesson-order.json` and the gate taken from main, the Friction evidence entry re-carried, records re-derived, admission re-cut). `static-contract` went red on the merge commit: `standalone/offline boundary violated by changed files: ['tools/easter/SCIENCE_ORIGINAL_TARGETS.json', 'tools/grow_resources/CONTENT.json']`. Measured: the same red stood on the three earlier PR heads (runs 34995265397, 35009104636, 35010475527) — not new from the merge, and not a flake. Cause: the admission tool pinned only `Science_Teesside/` paths, and the boundary reds on any MODIFIED file it does not pin. The original-targets ledger (carries the patched lesson's SHA-256) and the GROW resource content (the source the resource pages are built from) are review records, not served studios. Fix (this commit): `admit_w3_friction.py` admits both by exact digest through `REVIEWED_PATHS` → `CATALOGUE_PINS` (470 reviewed files, gate `f299b299ec2101091efc3d2a4eed3ec35e871c5161e914c335465cb63bb07b2d`; not added to `ALLOWED_DIFF`, not added to the GLV3 replacement block). Its `--check` mode previously always failed on the apps.json stand-in; it now pins in a scratch pass and compares the gate bytes. Local: cross-estate gate PASS with positive control (3 errors), `admit --check` PASS, PIN1 477 asserted, `check_packs.py` 25 routes, static check exit 0, companion catalogue 116, lesson order 1075, GLV3 self-test 430 controls, node syntax checks. CI on the fix pending; Ready still waits on S3.
- §3 Diffusion candidate `Science_Teesside/Launch/SCI_L_W4_L1_Diffusion.html` SHA-256 `563e0bddc7049afb1dfc7245b1810da5094c19bca8027a08bbaf4c26d5a0dc54` (258,861 bytes, worktree `claude/cx2-diffusion`, not yet committed): all-stage acceptance harness (`tools/launch_resources/lesson_acceptance.py`, the Friction harness parametrised) **PASS** — 108 stage cases (standalone + http × 320/390/768/1280), axe serious/critical 0 on every stage (moderate: landmark/region/heading-order on the shared chassis, as Friction), keyboard transitions 96, enlarged 8, themes 4, no-JS 4, prints 50 (KO and teacher-feedback sheets 2 pages by design), packs 6 × 4 pages, page errors 0. The ten console `ERR_FILE_NOT_FOUND` lines are the chassis's `/hud.js` reference under `file://`, present identically in the base 3a14e9c. Still owed before M-STOP S1: native parity (PPTX/PDF/DOCX), resource page, START_HERE and pack records, GLV3 transaction and pins, reading band per route, commit, draft PR Ready, the candidate message with the full hash.

## 8. Diffusion candidate and PR #546; Friction's FieldOps red fixed; Lane D tools merged and published; §4.8 findings — 15 September 2026
- **§3 Diffusion.** Candidate `Science_Teesside/Launch/SCI_L_W4_L1_Diffusion.html` SHA-256 `563e0bddc7049afb1dfc7245b1810da5094c19bca8027a08bbaf4c26d5a0dc54` (258,861 bytes). Native parity through `tools/launch_resources/author_w4l1_natives.py` (deck 17 → 21 slides: three arrival routes, two organiser slides, stage minutes 4/1/4/4/3/5/15/4 as the lesson, one optional help prompt, two-check exit; worksheet + organiser + exit pages; the teacher guide's W4L1 section carries the staff answers and the feedback card; PDFs re-rendered, the combined pupil worksheets rebuilt). `refresh_w4l1_pack_records.py` rewrote the LAUNCH manifest, checksum rows, download index and all 21 archives (the teacher guide travels in every archive). `admit_w4l1.py` declared the 36-member GLV3 transaction and pinned the members and the original-targets ledger (row: stage count 10 → 9, sha). Local: all-stage harness PASS (108 stage cases, axe serious/critical 0), original-Science browser run PASS (743 cases, 84 PDFs), GLV3 self-test PASS (519 controls), cross-estate gate PASS, `check_packs.py` PASS, reading band whole 7.5, routes 8.1/8.55/8.35 (ceiling 14.21). Draft PR **#546** (`claude/cx2-diffusion` d3e160be9184360fd800aeab69723a8a5d1a5110), stacked on the Lane D foundation (#542) because the transaction machinery lives there. LibreOffice Writer was absent from the container (only Impress/Draw); installed for the DOCX renders. M-STOP S1 message goes in the readback; not merged, not published.
- **#538 Friction.** `static-contract` green on 0295828 after entry 7's fix. The FieldOps job "Original Science navigation, one clock and pupil print" was red on every head: (a) `tools/grow_resources/BROWSER_TARGETS.json` pinned the pre-EDU-Q1 digests of GS_W3A/GS_W3B/CONTENT.json/the lesson; (b) the accepted lesson says "Resume Lesson B" and "Stop after Lesson A" where the check and the ledger expected "Resume period 2", "STOP for today" and "Tuesday P1 (09:30–10:10)". Fix 2d01bc2: four digests recomputed and the record admitted by digest; the check's two patterns and the ledger's `periodDays` accept the lesson-name wording as well as the original. Local full run PASS (743 cases; grow-resources 150/150; print review 84 PDFs; GROW print content complete). CI on 2d01bc2 pending. Ready still waits on S3.
- **Lane D.** #541 (tools) merged: main `bb8d7c79e6ff1157c30969e74ad8b603baa17011`; publication run 35020062533 succeeded (no public file changes; carrier 297ed5de unchanged). #542 (foundation) retargeted to main and brought up to date by merging main (ce166ba); its `static-contract` red ("reviewed catalogue bytes differ: _glv3/tools/verify_change_boundary.py") fixed by re-pinning the gate (7462312). A second foundation change (2b5acd9): a later declared transaction now supersedes the earlier claim on the same path (five new self-test controls) — needed so a second reviewed edit of a merged transaction's file (Sugar, below) can be declared at all. BUILD carrier prepared locally on the Site branch (`claude/cx2-r5-catalogue-publisher` 08f84452084391203b940a4d3619c4b3b6ecd91e: W8B served digest b95eafbc → `47364350d4769b89aeb3d36967bb89d705c486e19a09ca57750d7f1ba7873601`, lesson-order 0091727d → `afcc029ca56bcc579e64cb18d667f3442c0bc5bd192d65e78bd401e0a27aaf75`, measured by running the pinned publisher on the #543 tree); not pushed yet — pushed with #543's pin move, under Matt's authorisation for that branch. Note (0.3): a base-branch change does not start CI on a stacked PR; #542 needed a real commit (the merge of main) to run.
- **§4.8 hygiene sweep (three pilots, lesson + natives).** Sugar lesson (main) and its teacher DOCX name the evidence product three times in the staff feedback card ("the 16-page EFL version", "EFL capture should be lean…", "E — evidence on EFL") — R10 breach on a public, published file; `BUILD/SHA256SUMS.txt` carries the pre-correction digests for all six W8A files. Both fixed on `claude/cx2-sugar-r10` (own PR, stacked on the foundation; tool `tools/science_teaching_packs/cx2_sugar_r10.py`, idempotent, `--check`). Friction: 0 product names, 0 day words in lesson and four natives. Diffusion: 0 in lesson and natives; the shared LAUNCH teacher guide's Week 5–7 notes (other lessons) still say Wednesday/Friday — staff-facing, outside this transaction, Matt list. Timer displays (00:00, 15:00, 32:00) are not dates.

## 9. Sugar hygiene PR #547; BUILD carrier pushed and #543 pinned; foundation supersession rule; Diffusion rebased — 15 September 2026
- **§4.8 Sugar (R10).** PR **#547** `claude/cx2-sugar-r10` 0f146405dbaffc15931ae64c5fecf19f95cd6d74, stacked on the foundation: the three staff sentences and the teacher DOCX/PDF, `BUILD/SHA256SUMS.txt` (six stale W8A rows recomputed and the list pinned), its own GLV3 transaction "Sugar R10 hygiene" (four members, review base bb8d7c7) declared after the merged Sugar one. GLV3 self-test PASS (139 controls), gate PASS, PIN1 472, `--check` PASS. Needs its own Site carrier before merging (lesson bytes change).
- **Foundation (#542) rule change (0.3 finding).** Transactions were judged in isolation and each reddened on a partial diff of its members, so no second reviewed edit of a merged transaction's file could ever be declared. Now declaration order is review order: the last transaction naming a path owns it (`owned_members`); five self-test controls prove the retirement and that the later transaction still judges the path exactly. A first version of one control counted against the full member list and only passed while no real later transaction existed; corrected (8a993ae). `admit_w8.py --check` fixed the same way as the Friction and Diffusion admission tools (767f680). Foundation head `767f680dfda43dc8d8d3713ea77616639497bc5a`; CI re-running.
- **Lane D BUILD (#543).** Site carrier `08f84452084391203b940a4d3619c4b3b6ecd91e` pushed to `claude/cx2-r5-catalogue-publisher` (the branch Matt authorised; one registry commit on top of 297ed5de). #543 head `ca75740` merges the foundation and moves `education-pages.yml` `uses@`/`builder_ref` and the gate's caller digest (`1a5d373bf9f230e63a25be0eda8878e82fff5ffa6100b10988dbe30cf66f7d56`) to that carrier; `admit_w8.py --pathway BUILD` re-cut the pins; GLV3 PASS (1 protected change), gate PASS, `--check` PASS. Order: #542 merges (no public bytes) → publication green → #543 retargeted to main, main merged in, CI → merge → publication → served proof (UNMEASURED from the container; Matt's phone). GROW (#544) and LAUNCH (#545) carriers are built only after the preceding merge, because each re-derives `lesson-order.json` on top of the last.
- **Diffusion (#546)** rebased onto the foundation head; review base moved to `8a993ae1c82087150f9076be49395747f238decc`; head `b7bfb30d22a71e0827b54f401dfc52e1e68d1d2d`; GLV3 PASS (36 protected changes, 524 controls), gate PASS, `--check` PASS.

## 10. S3 ruled and implemented; #542 merged; S3 PR #548 with its carrier — 15 September 2026
- **#542 Lane D foundation merged**: main `5778ede06902e7a218459452ae79e3ce330db693`, every job green. #543 retargeted to main.
- **STOP S3 closed by Matt's ruling (21:5x, phone): relabel the whole set.** The ruling changed shape because of what the measurement showed. The order framed S3 as two stale Week 3 companions; measured on main, **all twenty** companion pages (BUILD and GROW, weeks 3–7) are a separate ~70KB chassis sharing **0 sampled text runs** with their 285,345–413,422-byte canonical lessons, in every week (GROW W3A 0/131, W4A 0/71, W5A 0/62, W6A 0/56, W7A 0/65; BUILD W3A 0/69). They were never exports and no generator exists in the repository or its history. Retiring Week 3's two entries would have left eighteen pages in the same condition, unnoticed only because those weeks' lessons had not been revised. Options were put to Matt with that evidence; Matt chose the estate-wide relabel.
- **PR #548** `claude/cx2-s3-offline-edition` head `c1b1344`: one content file, `Science_Teesside/Teaching_Packs/web-slides.html`. Each pathway sentence now states this is a lightweight offline edition and not the full lesson; each of the twenty entries links its canonical lesson; the first link is renamed so "web slides" is not read as the lesson. No page deleted, no route removed, no companion or pack file touched. Wording claims no feature a given lesson may not have (arrivals/organiser/new exit exist on Friction only so far). Tool `tools/science_teaching_packs/cx2_s3_offline_edition.py`, idempotent, `--check` PASS. Own GLV3 transaction "S3 offline edition labelling"; GLV3 PASS (1 protected change, 138 controls); gate PASS; `check_packs.py` PASS; PIN1 472; all twenty canonical links resolve; no unresolved local href.
- Instrument note (0.3): the hub's first relabel pass silently renamed nothing because the pattern matched `&rarr;` while the file stores a literal `→`; caught by counting occurrences afterwards rather than trusting the run. `check_hub_browser.cjs` fails identically on unmodified main in this container, so it is not evidence either way locally; CI runs it.
- **Site carrier `a89e2c601b491be7b0121754a17f56228eafa21a`** pushed to `claude/cx2-r5-catalogue-publisher`: adds the hub pair (`4042f4ac` → `30af175e`) beside the Lane D BUILD pairs. Every entry keeps its published rollback digest, so the carrier admits the current served tree whichever PR merges first. Control: the same local publisher run on main reproduces the admitted hub digest `4042f4ac` exactly. #548 moves `education-pages.yml` and the gate's caller digest (`9079af3bd47a38ddaa801826ddf663f66d6c4f7a0eca15eb962e52ee2f2b377f`) to it.
- Merge order from here: #548 (S3) → publication → #538 Friction Ready → CI → merge → publication → served proof → §2.5 → then Lane D #543 → #544 → #545, each with its own carrier.

## 11. EDU-HERO-20260915 and EDU-TRY-LESSON-20260915 read and recorded; #548 merged — 15 September 2026

**Source admission.** Matt supplied the Education master order twice. The copies are NOT the same file and the second is not the later one:

| Upload | SHA-256 | Bytes | Lines |
|---|---|---|---|
| `72c9a98b-…v2.md` (first) | `2209ab83cce586bc47ab6414acbef9de19c909bd3c242dfb762f80983a42138f` | 430,421 | 2,809 |
| `eaea9ef1-…v2.md` (second, sent as "the latest") | `24129d3f9c18899f69bdbb6abb25ed3497356cc5f8649c2a6af2b155b04f4f47` | 373,187 | 2,538 |

Measured: the two are byte-identical to char 373,131 and then the second simply **stops**, mid-section, inside EDU-HERO's own "Acceptance and handover" paragraph. It is a truncated copy, not a newer revision. It is missing 18 sections the first carries, including `EDU-TRY-LESSON-20260915` (absent entirely: 2 mentions vs 0) and all fourteen 15 September entries, and it adds nothing. **The first upload is the document of record here**; both amendments below are recorded from it. Flagged to Matt.

**EDU-HERO-20260915 — approved Education homepage and Teachers imagery. Status APPROVED / QUEUED as EDU-D3.**
- Placement: after EDU-Q1 acceptance, before PLAY-D2; homepage imagery first, Teachers second; coordinated with EDU-HEADER-BRAND-20260915. Remaining order EDU-Q1 → EDU-D3 → PLAY-D2 → PLAY-Q1 → PLAY-I1 → evidence-justified MAINT-1 → PLAY-X1.
- Homepage asset: Matt's supplied owl-and-learning-resources `27988.jpg`, SHA-256 `c6c1b9e8d6fb78b4b044d521cd75cef389e8b2a5a3f478f95ec718b79932b248` — the owl among the learning materials, not the earlier woodland-only concept.
- Teachers asset: `exec-c1f6aed3-0463-4f3a-addf-c22a5c9a7b03.png`, board title "04 / LEARNING COMES TO LIFE", SHA-256 `4cdfedd8b48fa7efdc0debd6a7c332a9fba0e4547c147291d46bab0d0360e77e` — teacher and three teenage learners, people the focus.
- Refinements Matt re-confirmed in chat and which the amendment already carries: breathing room around the owl; prism away from its face; simpler foreground, especially on phone; no supplementary slogans inside the artwork, notebook or tablet; original approved metallic silver circular M; cream, midnight navy and sage; the existing approved header amendment (MADE BY MATT on one line including mobile, Learn • Build • Explore beneath).
- Invariants: keep the accepted headline "Big on ideas. Light on prep." and real page copy — the mockups authorise no copy rewrite; keep real subject labels, card destinations, catalogue facts and pathways; device frames and board titles are references and must not be embedded; alt text by purpose, and never label generated classroom subjects as actual staff, pupils or testimonials.
- Acceptance owed at implementation: 320/390/768/1280 and enlarged text on phone, tablet and desktop; contrast; legible owl/interaction; no overlap or horizontal overflow; responsive compressed assets with reserved dimensions recorded; served pages and assets verified after the release gates.

**EDU-TRY-LESSON-20260915 — approved science showcase rotation. Status APPROVED / QUEUED within EDU-D3.** Featured set in order: BUILD W8A Sugar, GROW W3A Friction (preserving the paired W3B source and shared timing), LAUNCH W4L1 Diffusion (preserving its shared guide). Eight-second gentle rotation from Sugar; labelled Previous/Next/Pause-Play plus a position indicator; rotation stops on keyboard focus or manual selection and resumes only by explicit Play; pause on hover and when hidden; reduced-motion starts with rotation off; no focus moves, no automatic live announcements, no focusable controls inside inactive slides; a useful linked Sugar card without JavaScript. Card position and cream/navy/sage design unchanged.

**Eligibility measured today.** The amendment admits only accepted AND published EDU-Q1 identities. Sugar is published. Friction (#538) is held and unmerged; Diffusion (#546) is accepted by Matt's listening pass but unmerged and unpublished. So **one of three is eligible right now**, and the amendment's own rule makes that a static card with no carousel controls. Implementing the rotation before Friction and Diffusion publish would either ship a degenerate one-item carousel or promote unfinished drafts, which the amendment forbids. Both amendments are therefore recorded and queued, and EDU-Q1 continues first, exactly as each handover paragraph instructs.

**#548 merged** — main `28d51c8e73328d668b4358a1b99226d3f8a7769f`, all nineteen checks green. That lands the S3 offline-edition relabelling. Publication run follows; the carrier `a89e2c60` admits both the rollback and the new hub digest, and the registry is fail-closed, so a mistake blocks publication rather than serving wrong bytes.

## 12. EDU-D3 concept assets received and identity-verified; Play board does not match its approved reference — 15 September 2026

Matt supplied three concept boards. Identity checked by digest against the master order rather than by appearance:

| Board | SHA-256 | Order's recorded identity | Verdict |
|---|---|---|---|
| "02 / IDEAS COMING TO LIFE", owl and resources | `c6c1b9e8d6fb78b4b044d521cd75cef389e8b2a5a3f478f95ec718b79932b248` | homepage asset `27988.jpg` | **exact match** |
| "04 / LEARNING COMES TO LIFE", classroom blend | `4cdfedd8b48fa7efdc0debd6a7c332a9fba0e4547c147291d46bab0d0360e77e` | Teachers asset `exec-c1f6aed3-0463-4f3a-addf-c22a5c9a7b03.png` | **exact match** |
| Play, single neon controller, "Pick a game. Make it yours." | `f2d77fa5442d2fddc1dd1507755d7f7f64d12c779e907898f092f38fa8222811` | PLAY-D2 approves the **LEFT** controller inside the two-controller comparison `exec-73468ce7-8fc0-452e-872c-bceb9c7a2a2f.png` | **no match** — different asset |

**Both Education boards are the approved assets.** EDU-HERO's authoritative homepage reference `27988.jpg` is therefore this device-mockup board, not a bare photograph. Since the same amendment rules that "device frames, mockup page text and presentation-board titles are references only and must not be embedded into the live page", the homepage hero must be **extracted** from the composition inside the board's desktop frame — the same shape as PLAY-D2's "extract the left composition only". Recorded so implementation does not embed a picture of a monitor.

**Refinements now visible in the approved homepage board, and owed at implementation.** Each is already required by EDU-HERO; this records where each one bites:
1. The prism sits directly against the owl's face and breast. Move it clear and restore breathing room around the owl.
2. Foreground carries globe, pencil pot, open notebook, prism, tablet and a stack of four books at once. Simplify, especially at phone width.
3. Two slogans are inside the artwork and must go: a handwritten "Small steps brighter tomorrows" across the notebook page, and "A BRIGHTER HUMAN FUTURE" on the tablet screen.
4. Already correct and to be preserved: the headline "Big on ideas. Light on prep.", the eyebrow "MADE BY A TEACHER. BUILT FOR REAL CLASSROOMS.", MADE BY MATT on one line with "Learn • Build • Explore" beneath, and the silver circular M.
5. The board's subject cards read Science / Humanities & RE / Art Studio / Lifeskills with BUILD / GROW / LAUNCH chips. These are mockup labels: confirm every label, destination and pathway chip against the real catalogue before use, because the amendment forbids mockup content replacing verified records.

**Teachers board.** Matches the approved allocation (teacher and three teenage learners, prism, tablet, shared notebook). Two carries: the wall art reads "Curious Kind Brighter Together", which is a supplementary slogan inside the artwork and falls under refinement 3; and the people are generated, so alt text must describe purpose and must never present them as actual staff, pupils or testimonials.

**Play board.** Received and recorded, but its digest does not match the approved PLAY-D2 reference, and PLAY-D2 sits after EDU-D3 in the order. Treated as a further concept, not as the approved asset; no PLAY work started and none claimed. To be put to Matt at the readback: whether this supersedes the LEFT-controller extraction from `exec-73468ce7…`, or sits alongside it.

Nothing is implemented from any of these. EDU-D3 remains queued behind EDU-Q1, per both amendments' own handover paragraphs.

## 13. EDU-HEADER-BRAND-20260915 read and recorded; three order copies reconciled — 15 September 2026

**EDU-HEADER-BRAND-20260915 — approved header tagline and single-line name. Status: approved and queued; implementation and live verification pending.** Requested by Matt on 15 September 2026 from mobile screenshots of madebymatt.uk, where the footer showed both lines correctly but the header wrapped the name after "BY".

- Keep **MADE BY MATT** together on one line in the Education page header, including phone layouts.
- Add **Learn • Build • Explore** beneath the logo/brand block at the top of the page, rendered as readable text, with the original approved M artwork intact.
- Apply consistently to the shared Education header and its owning templates. Retain the existing footer branding.
- **This supersedes the earlier Education rule prohibiting a header tagline or restricting that wording to the footer.** Play branding and Play copy are unaffected and remain separately approved.
- Give the brand room through responsive layout, repositioning header actions if needed. Preserve Saved, search, Menu and the four navigation links, readable text and usable touch targets. Wrapping must not be solved with clipped text or blocked zoom.
- Acceptance: desktop and mobile at 320 and 390 CSS pixels and enlarged text; the complete name stays on one line; the tagline sits underneath; no control overlap and no horizontal overflow; the served header verified after the normal release checks.
- Scheduling: a bounded follow-up owned by the Education shared-header owner, queued for the next suitable Education chrome checkpoint, coordinated with EDU-HERO-20260915 in EDU-D3. It does not expand an already-checked release candidate without owner review, and claims no website change.

**Cross-check against the approved artwork.** Both concept boards verified in entry 12 already render this header correctly: "MADE BY MATT" on one line with "Learn • Build • Explore" beneath the silver circular M, on phone, tablet and desktop frames alike. So the amendment and the approved imagery agree, and EDU-D3 can implement them together as the order intends.

**Three copies of the master order, reconciled by digest.** Matt has now supplied the order three times, each sent as the latest. Measured, they run oldest-last:

| Upload | SHA-256 | Bytes | Lines | Carries EDU-HERO amendment | Carries EDU-TRY-LESSON |
|---|---|---|---|---|---|
| `72c9a98b` | `2209ab83cce586bc47ab6414acbef9de19c909bd3c242dfb762f80983a42138f` | 430,421 | 2,809 | yes | yes |
| `eaea9ef1` | `24129d3f9c18899f69bdbb6abb25ed3497356cc5f8649c2a6af2b155b04f4f47` | 373,187 | 2,538 | truncated mid-section | no |
| `cc039887` | `a5756b8b8acccdd84798e9814e4e09b32784898d0961de6444e22824a1a69a96` | 347,012 | 2,416 | no | no |

The second is byte-identical to the first for 373,131 characters and then stops mid-sentence. The third diverges from the first at line 3 and is missing twenty-four sections, among them the whole EDU-HERO amendment. Each is a subset of the first; none adds anything the first lacks. **`72c9a98b` (`2209ab83…`) is the document of record for this session**, and all three amendments have been recorded from it. EDU-HEADER-BRAND survives in all three copies, so this one amendment was never at risk; EDU-HERO and EDU-TRY-LESSON would have been lost had the later uploads been trusted. Raised with Matt so the export that produced the short copies can be checked.

**Merge-hold note.** The master order records an EDU-Q1 08:30–15:30 UK school-day merge hold. ORDER CX2 R2 lifted it in favour of one merge at a time with publication settled between, which is the rule being followed; the merges made tonight also fall outside that window in any case.

Nothing implemented from EDU-D3. EDU-Q1 continues first, as every one of the three amendments instructs in its own handover paragraph.

## 14. PLAY-BRAND-20260915 read and recorded; the approved controller comparison supplied and hashed — 15 September 2026

Source: `PLAY_Brand_Amendment_2026-09-15.md`, SHA-256 `3abbace35ffa8c6183830bd572a1582af3e730a3b70fcf176ee350c603fa8860`, 11,016 bytes. Status **APPROVED / QUEUED — implementation and publication outstanding**. Nothing in Play is started, changed or claimed.

**The approved artwork is now in hand, and its identity is recorded.** Matt supplied the two-controller comparison: left an Xbox Series X–style pad with dark sculpted body, asymmetric sticks, faceted circular D-pad, ABXY buttons, three-quarter clockwise tilt, cyan/mint left rim, violet right rim and a restrained sparkle constellation; right a white PS5 DualSense. That matches the amendment's description of `exec-73468ce7-8fc0-452e-872c-bceb9c7a2a2f.png` exactly. **SHA-256 `3869564854869fa0cea53d6ac40ec95034c91596eda61baee22ebbe476ede29c`, 1,831,822 bytes**, recorded here as the working identity.

Gap worth naming: the amendment identifies this artwork **by filename only** and records no digest for it, while instructing that an unrelated controller or a regenerated approximation must not be treated as the exact approved source. Filename alone cannot enforce that; a digest can. The hash above closes it, and supersedes the earlier single-controller board (`f2d77fa5442d2fddc1dd1507755d7f7f64d12c779e907898f092f38fa8222811`) recorded in entry 12, which is a different asset and is **not** the approved reference.

**Sequence discrepancy, reconciled.** This standalone amendment states the remaining order as EDU-Q1 → PLAY-D2 → PLAY-Q1 → PLAY-I1 → MAINT-1 → PLAY-X1, with no EDU-D3. The master order's 15 September entries state it consistently **with** EDU-D3: EDU-Q1 → EDU-D3 → PLAY-D2 → PLAY-Q1 → PLAY-I1 → evidence-justified MAINT-1 → PLAY-X1, and EDU-HERO schedules EDU-D3 explicitly "after EDU-Q1 and before PLAY-D2". The master order's entries postdate and incorporate both amendments, so **the authoritative sequence includes EDU-D3** and the standalone file's line is superseded on that one point only. Raised with Matt rather than resolved silently.

**PLAY-D2 — approved Xbox homepage hero.** Left controller only; the PS5 pad is an unselected comparison and must be excluded when extracting a standalone asset. Top-right decorative illustration in the existing hero. Keep the charcoal background and the wording "YOUR NEXT GAME STARTS HERE." / "Pick a game. Make it yours."; search, filters, shelves and primary actions keep their roles. The illustration must not become a second large banner or push games down the phone layout; compact it or omit it at the narrowest widths. Deliver an optimised locally served WebP with fallback, explicit intrinsic dimensions and responsive sizing, checking the real hero background so no rectangular edge shows; record final source hash, dimensions and transferred size. Static and decorative: no autoplay, flashing or animation, and empty alt text. The Play logo stays separate and unchanged; the controller is not a brand mark and implies no controller compatibility. Acceptance at 320/390/768/1280 and enlarged text, with no silhouette crop, overflow, hidden search or overlapping actions; served asset and homepage verified after owner-managed publication. Closes independently of the splash rollout.

**PLAY-Q1-SPLASH — unified launch branding.** Chosen supporting line is "YOUR NEXT GAME STARTS HERE."; "Pick a game. Make it yours." stays in the catalogue hero, and splash taglines do not alternate between games. Logo authority is the updated Play mark already accepted and served by PLAY-D1, from Matt's `27961.jpg`, source SHA-256 `bfef5b1ee9606351f6185aad5549f5252ebc4918dcb939fce72d3cbe9c792d2a`; preserve its shape, circle, star and supplied colours, and never substitute the generated controller's Xbox symbol for the Play mark. One compact centred lockup, the chosen line, and a genuine loading status only while loading. Derive the current catalogue count and routes rather than hard-coding the last baseline of 69, keep a per-route coverage table, reuse the existing Maker splash canon, make the smallest shared-template change with explicit adapters, and never stack a new splash above an old one. Tie indicators to real readiness: indeterminate when progress is unmeasurable, never invented percentages, forced delays or a looping splash after ready; readable recovery on asset failure; respect reduced motion and the existing audio-start gesture; restore focus when the splash leaves and let no invisible overlay intercept input. Pilot one route per delivery mechanism, then roll out in small named batches. Preserve mechanics, routes, save keys and formats, pause/audio/Comfort controls and the AS1 HUD. No estate-wide completion claim while any route is unverified.

**Holds carried.** The amendment keeps the 08:30–15:30 UK school-day merge rule and the Site #291 / Lessons #456 / LP1 holds "unless separately and explicitly overridden". ORDER CX2 R2 is exactly such an explicit override of the merge hold, and is the rule in force; tonight's merges also fall outside that window. The Site and Lessons holds are untouched by anything in this session.

**Access note for implementation.** Play and Games live outside this session's repository scope, which is `mattroper1977/lessons`. PLAY-D2 and PLAY-Q1-SPLASH will need those repositories attached before any work, and none has been attempted.

## 15. BLOCKER B2: served digests for changed lesson HTML cannot be measured in this container — 15 September 2026

Friction's carrier work exposed this, and it invalidates a digest already written into a carrier. Found by running the control the order requires (0.3: a green run that did not exercise the gate is not evidence) rather than trusting the first build.

**Method under test.** Candidate served digests were being computed by running the pinned publisher locally: `build_education.py --lessons <tree> --output <dir> --allow-sparse`. The sparse flag is needed because this container has no Apps checkout.

**Control.** Build Lessons **main** the same way and ask whether each output file reproduces a digest the carrier already admits. Main publishes green, so a faithful build must reproduce every one.

| Type | Reproduce an admitted digest | Faithful |
|---|---|---|
| PDF | 773 / 773 | yes |
| DOCX | 415 / 415 | yes |
| PPTX | 246 / 246 | yes |
| ZIP | 113 / 113 | yes |
| JSON | 112 / 112 | yes |
| SVG | 112 / 112 | yes |
| TXT | 91 / 91 | yes |
| PNG | 80 / 80 | yes |
| CSS | 34 / 34 | yes |
| **HTML** | **559 / 1503** | **no** |
| Overall | 2797 / 3742 | |

**What was ruled out.** The publisher's only HTML transformation is `with_lesson_navigation`, which inserts exactly 76 bytes (`<script defer src="/Lessons/assets/catalogue/lesson-navigation.js"></script>`) before the last `</body>`, deterministically, and returns the text unchanged when the adapter is already present. Byte-diffing source against output confirms that insertion and nothing else. `build_publications.py`, the step the caller runs first, was also run: it writes to its own output directory and leaves the Lessons tree unmodified (0 files changed). For the 944 mismatching HTML files the admitted digest equals **neither** the source digest nor the source-plus-injection digest, so the difference is not the injection and not a stale source. The remaining difference between this run and the live one is the Apps source, which the live caller supplies and this container cannot.

**Consequence, and the correction made.** The BUILD W8B candidate digest `47364350…`, written into Site carrier `08f84452` and carried into `a89e2c60`, was produced by this method and is not trustworthy. Site carrier **`e1645d384ef88c9d761c18c91794f8624813b99c`** withdraws it and restores W8B's original rollback pair. Nothing independently confirmed was touched: the teaching-pack hub pair stands because publication run 35025572232 succeeded on the carrier carrying it, and the catalogue JSON entries are of a type the control reproduces exactly. No fabricated identity remains in the chain.

**BLOCKER B2.** Any pull request that changes **lesson HTML bytes** cannot have its served digest measured here, so it cannot be given a correct carrier from this container. That is #543 Lane D BUILD (now returned to draft), #538 Friction, #546 Diffusion and #547 Sugar. Work that changes only non-HTML public files is unaffected, and #548 already shipped on that basis.

Routes out, for Matt to choose: (a) attach the Apps repository to this session so a full non-sparse build can run; (b) measure the digests in a CI job on the real publisher and read them back; (c) accept a deliberate first publication failure per pathway and take the digest the publisher reports from its own refusal. Option (c) needs no new access and is self-correcting, but it spends a red publication run each time. Nothing is guessed in the meantime.

## 16. Census route, disk, audit findings, order fix — 15 September 2026, ~22:20 UTC

**Approvals recorded (Matt, this window):** "You can choose to merge more than one at a time … approval to fix the master orders order and other issues that may arise … don't pause between passes"; "Yes carry on, merge 549 and keep going"; "Fix the standalone PLAY amendment omits EDU-D3"; "Follow through all orders without pausing to the end you have my approval to do anything necessary to do this." The two-controller comparison image was re-supplied twice more; both copies hash `3869564854869fa0cea53d6ac40ec95034c91596eda61baee22ebbe476ede29c`, the copy already recorded in entry 14.

**#549 merged** (`423f11d9`, 8/8 checks green) — the served-digest census workflow is on main. Dispatched on `claude/cx2-diffusion` (run 35029074233) and `codex/edu-q1-grow-w3a-focus-20260915` (run 35029124992) at builder `e1645d3`. **Both failed in ~90 s, identically:** `build_education.py` ends by running the all-file admission census against the carrier registry and raises on the first problem — here `CHANGED education-site/data/usage-registry.json`, the usage-discovery table that any lesson batch moves. That is the expected state of a candidate (its bytes are not admitted yet — measuring them is the point), and the trees are fully written before the raise, but the job stops before the digest step. A patch making the census read the tree regardless and report every CHANGED/UNREVIEWED/MISSING row with its built digest was **refused by the session's action classifier as a CI bypass**; I did not work round it. Route (a) from entry 15 was taken instead.

**Route (a), local full build.** The pinned Apps commit `3ad0a7df958274e5401e8d40d0a94096be0962bc` was fetched into the local Apps clone (the clone's head `e15f6e39` is newer and its Creator Hub anchor differs, which is why the earlier non-sparse attempt failed). With Apps at the pin and no `--allow-sparse`, the publisher passes its full-source check and runs every refresher. A control build of Lessons main `423f11d9` at builder `e1645d3` is running; **it must reproduce an admitted digest for every file** before any candidate digest from this method is written into a carrier. Result in the next entry — not claimed here.

**Disk.** The container's writable allowance filled (six 840 MB local publication trees plus four stale worktrees); the first control build died on ENOSPC. Removed the stale trees and the merged worktrees (census, s3, laned-foundation, main-wt, ctl2, lessons-929): 12 GB free.

**Branches merged with main (`423f11d9`).** `claude/cx2-diffusion` → `e3455652` (four generated/declaration files taken from main and re-derived; `admit_w4l1.py` now excludes paths another declared transaction owns, the shape `admit_w3_friction.py` set), `ea251665`, `b0138f4d` (acceptance record `docs/edu-q1/LAUNCH_W4L1_PARITY_ACCEPTANCE_2026-09-15.md`); boundary gate PASS 36/0, cross-estate static contract PASS. `codex/edu-q1-grow-w3a-focus-20260915` → `d7b3289e` (clean merge; admit re-run changes nothing; boundary PASS 28/0; cross-estate PASS).

**Adversarial audit (`wf_cdc84de5-a06`), five of six dimensions in.** Confirmed and acted on: every open candidate pins a carrier that admits none of its public files (already known — that is the carrier work); `assets/catalogue/lesson-order.json` is a two-digest window, so **candidate merges are sequential by construction** (registry `validate_digest_set` allows at most two): each branch merges the then-current main, rebuilds, and gets its own carrier; "merge more than one at a time" is therefore bounded to what the registry can hold. `Science_Teesside/Teaching_Packs/index.html` is a member of both Diffusion W4L1 and GROW W3 Friction from the same base blob — the second to merge must re-derive the hub (`build_hub.build()`), move its review base to the post-merge main and re-admit. #543's pin/caller conflicts with main are resolved by taking main and re-pinning when its carrier is minted. Recorded for the hygiene pass, not blocking: `glv3-verify.yml` runs the boundary fence only on PRs that touch `_glv3/**` (no PROTECTED prefix in its `paths:`) and never on main; the gate's `--self-test` has no negative control for `PUBLICATION_CALLER_SHA256`; Sugar's superseded `SUGAR_REPLACEMENTS` rows carry inert stale digests; the transaction-entries marker block is a one-line merge hot-spot (resolution rule now used: take main, re-run the admit tool, never `--ours`).

**Order fix.** The standalone PLAY amendment's "Remaining order" omitted EDU-D3. Corrected copy committed as `docs/orders/PLAY_BRAND_AMENDMENT_2026-09-15.md` (`544207b2…`), and a corrected master-order copy (`e381a792…`; the two PLAY-BRAND lines that still stated the older order as current, lines 7 and 2316) returned to Matt. Dated checkpoint lines were left as history. The refinement brief and PLAY-D1 checkpoint copies were not uploaded, so they are not fixed here.

## 17. Full-build method proven; Friction carrier minted and pinned — 15 September 2026, ~22:40 UTC

**Method qualified by control.** The publisher was run locally as a **full** three-repository build (Site builder `e1645d3`, Apps at the publisher's pin `3ad0a7df`, Python 3.12.3 in a venv with lxml 6.0.1 — the container's default 3.11 cannot even import the builder's front-door module, an f-string the 3.12 grammar allows). A control build of Lessons main `423f11d9` **passed the builder's own all-file admission** (exit 0) and, censused independently, reproduces an admitted digest for **every one of its 4044 files (0 CHANGED, 0 UNREVIEWED, 0 MISSING)** against the carrier registry. That is the faithfulness proof the sparse method failed (entry 15: 559/1503 HTML). B2 is closed by route (a); the CI census workflow stays on main as a second instrument but is not needed for this.

**Friction carrier.** Full build of `codex/edu-q1-grow-w3a-focus-20260915` at `29f3bc7f` → 30 paths differ from the served tree, 0 added, 0 removed: the lesson, GS_W3A/GS_W3B, the three GROW pack records, seven archives, W3A/W3B natives (PPTX, slide PDF, pupil/teacher DOCX+PDF, START_HERE), the hub index, `assets/catalogue/lesson-order.json` and education-site `data/usage-registry.json`. Site carrier **`8cfd08b3abe08eeb79ff2139bedf07fbf536c520`** (registry-only, on `e1645d3`) writes each as `[served rollback, candidate]`; the Lane D BUILD lesson-order candidate `afcc029c` is displaced from that pair (re-minted when that branch is rebased); the W8B rollback pair is untouched. Verified before commit: the Friction build is green against the new registry **and** the main build stays green. Pushed to `claude/cx2-r5-catalogue-publisher` only.

**Pin moved on this branch:** `education-pages.yml` uses@/builder_ref `a89e2c6 → 8cfd08b3`, `PUBLICATION_CALLER_SHA256` re-cut, admit tool re-run (static check, CATALOGUE_PINS, PIN1), cross-estate static contract PASS. #538 goes Ready; merge when CI is green; the publication run and Matt's served proof follow. Then Diffusion: merge the post-Friction main, rebuild the hub, move its review base, re-admit, full build, carrier, pin — the same shape, one branch at a time (the registry holds two digests per path).

## 18. Friction merged; first publication red at the reviewed-rows check; fixed on the carrier — 15 September 2026, ~23:00 UTC

**#538 merged** at `acae624f` (19 check runs green). The Education publication (run 35031195710) **failed** — not at admission (every built file matched carrier `8cfd08b3`) but at `check_education_separation.py`: *"Download additions differ from the reviewed installed pack: /Lessons/Science_Teesside/Teaching_Packs/"*. The publisher pins a reviewed row set (`science-download-usage-additions.json`, digest-pinned in the check) that must equal the built usage registry's Science Teaching_Packs rows exactly; each row's title carries the download's size in KB/MB, so the nine re-rendered Friction files moved nine rows. Reproduced on the local full build of `acae624f` (same failure), then fixed: Site carrier **`17e82d70`** reviews the nine titles to the built sizes and moves the pinned digest `266199e1 → 2cde72e7`; separation check PASS and `--self-test` PASS on that build; admission unchanged. This pull request moves main's pin `8cfd08b3 → 17e82d70` so the publication re-runs; **Merged ≠ served — Friction is not served until that run is green and Matt's phone shows the new bytes.**

**Diffusion.** `claude/cx2-diffusion` merged the post-Friction main at `ff87d139`: the hub rebuilt from records, the review base moved to `acae624f`, and `admit_w4l1.py` now claims a path another transaction owns only when this tree changes it again (the hub after Friction's claim), so the declaration-order supersession rule moves it to W4L1 and Friction keeps exactness over its 27 remaining members; boundary gate PASS 36/0 after commit (the rule reads previous identities at the merge-base, so it must run on a committed merge). Full build → 38 paths differ from the Friction tree; Site carrier **B** (on `17e82d70`) admits them as `[Friction, Diffusion]` pairs and reviews thirteen LAUNCH download rows. Because the rows check admits exactly one tree, B is pinned only by the Diffusion branch; main keeps `17e82d70`. Verified: Diffusion build admitted green on all three trees and separation PASS; the Friction build stays admitted.

**Lesson for the remaining branches (Sugar, Lane D):** every pack change moves its rows — the carrier for each branch now carries its registry pairs *and* its reviewed rows (helper `sync_science_additions.py`, which refuses added or removed routes), and each is pinned only by its own branch, in sequence.

## 19. Friction pin moved on main; Diffusion re-based on it — 15 September 2026, ~23:25 UTC

**#550 merged** at `5c562633` (17 checks green): main pins carrier `17e82d70` (the nine reviewed Friction rows). Its first CI run caught a second gate fact worth keeping: the cross-estate static contract reds on any *modified* file it does not list, and `docs/orders/CX2_CHECKPOINT.md` had only ever been *added* (#538) — so #550 also lists the four CX2 order ledgers in `ALLOWED_DIFF`, in the shape the UX2 ledger uses (documentation only, none served, none a studio). My local gate had passed because it ran before the checkpoint append — a real false pass; the rule now is to run the gates on the committed tree only. The publication run on `5c562633` is being watched; **Friction is served only when it is green and the served bytes are confirmed.**

**Diffusion (#546)**, 19/19 checks green on `88a4f021`, now takes main `5c562633`: the pin conflict (main → `17e82d70`, this branch → `7f97cbbf`) is resolved by taking main's files and re-running `move_pin.py` to carrier B and the admit tool, so the branch pins one commit on from main's carrier. It merges after Friction's publication is green.

## 20. Friction SERVED; Diffusion merged; Sugar carried — 15 September 2026, ~23:20 UTC

**Friction is published and deployed.** #550 merged at `5c562633`; publication run **35033008560 green on both jobs** — `publication / build` (including the step *"Prove all-file admission before packaging any education tree"*, which is the one that refuses to package a byte the carrier does not admit) and `publication / deploy` (`actions/deploy-pages@v4`, completed 23:00:21). So the bytes that went out are the reviewed Friction candidate digests, by the publisher's own refusal rule rather than by my assertion. Served digests now expected: lesson `5cea62e1…` (was `1a9b722d…`), GS_W3A `c57f5883…`, GS_W3B `89ecf578…`, W3A PPTX `13dceb4a…`, lesson-order `79b555be…`. **One thing is still genuinely UNMEASURED from here:** outbound access to the live host is blocked by this environment's network policy (the proxy returns 403 on `madebymatt.uk` and `mattroper1977.github.io`), so the final hop — CDN actually serving it — is Matt's phone to confirm, not mine.

**Diffusion merged** at `a8b3c968` (19 checks green) under carrier `7f97cbbf`; publication run 35033877773 watched. Its carrier was then independently confirmed correct by an unrelated measurement: the control build of main `a8b3c968` reproduces every digest carrier `7f97cbbf` admits, 4044 files, 0 problems.

**Sugar (#547)** retargeted from the merged Lane D foundation to main, merged `a8b3c968`, review base moved, re-admitted: boundary PASS 4 protected changes / 0 errors. Full build → **5 paths** move (the lesson, its teacher DOCX and PDF, the BUILD checksum list, lesson-order). Carrier **`d76182f4`**. Worth recording because it is the first candidate where the reviewed download rows did **not** move: the two re-rendered documents keep their KB-rounded sizes, measured at 0 of 320 rows changed rather than assumed — so the row sync is a real instrument, not a ritual.

**Lane D, order correction.** #543 (BUILD), #544 (GROW) and #545 (LAUNCH) are *stacked*: #544's base is #543's branch and #545's base is #544's. #545's tree therefore already contains all six return-week lessons. Merging #545 lands the whole stack in one commit and auto-closes the other two, so Lane D takes **one** carrier, one pin and one publication rather than three. Under Matt's approval to merge more than one at a time and to fix ordering problems, that is the route.

## 21. Sugar merged; Lane D carried as one; two instrument findings — 15 September 2026, ~23:55 UTC

**Sugar (#547) merged** at `ec7d34ab` (19 checks green) under carrier `d76182f4`; publication run 35034960811 watched. The control build of main at that commit is clean against `d76182f4` (4044 files, 0 problems), which independently confirms the carrier.

**A served-proof instrument that works from here.** `tools/served_proof.py` (scratch, to be committed with EDU-D3). The container cannot reach the live host — the proxy returns 403 for `madebymatt.uk` — so "fetch and hash" is unavailable, and the repository's older byte-check compared unlike bytes. What IS readable is the chain GitHub records: the publication run, the step *"Prove all-file admission before packaging any education tree"* (the publisher refuses to package a byte that does not match a reviewed digest, so a green admission means the packaged bytes ARE the reviewed ones), the package step, `deploy-pages`, and the **github-pages deployment for that exact sha with its status and `environment_url`**. Friction `5c562633` and Diffusion `a8b3c968` both return `state=success, environment_url=https://madebymatt.uk/Lessons/`. The tool states what it still cannot see: edge propagation, and what a given browser on a given network receives. That is a real narrowing of a long-standing UNMEASURED, not a claim of a fetch.

**Lane D as one carrier.** `claude/cx2-lane-d-launch` already contains the BUILD and GROW commits, so the stack merges once. Merged main, moved the shared review base, re-admitted all three pathway transactions (BUILD 1 member, GROW 2, LAUNCH 3): boundary PASS 6 protected changes / 0 errors. Full build → **7 paths** (six lessons + lesson-order), no usage-registry move, 0 of 320 download rows changed. Carrier **`429dd4ec`**.

**Two findings that each would have cost a cycle.**

1. *The boundary gate counts modifications, not additions.* `tools/rw1/admit_w8.py` redded the cross-estate static contract the first time it was touched after the Lane D foundation merged — while it was new it passed silently. That is the same asymmetry that bit `docs/orders/CX2_CHECKPOINT.md` at #550. Fixed as a class, not an instance: all six admission and staging tools are now listed in `ALLOWED_DIFF` with the reason recorded there.

2. *The carrier worktree had silently detached.* While the branch ref and the remote were both correctly at Sugar's carrier `d76182f4`, the working tree sat at detached `7f97cbb`, one commit behind. Nothing complained. The control build caught it by reporting exactly Sugar's five paths as CHANGED. Had it gone unnoticed, Lane D's carrier would have been minted on the pre-Sugar registry and silently dropped Sugar's rows. `mint_carrier.py` now refuses to read or write a registry unless the tree is on the branch, at the branch tip, matching origin and clean — red-proved by detaching the tree and watching it refuse.

**The withdrawn W8B digest is vindicated.** Carrier `08f84452` once admitted W8B as `47364350…`, computed by the `--allow-sparse` method; entry 15 withdrew it as untrustworthy. The full three-repository build of the same source bytes gives **`b67be20d…`**. Different — so the sparse digest would have failed this very publication on that exact file, and the withdrawal was correct rather than merely cautious.

## 22. Lane D: CI caught a real defect in all six lessons; corrected carrier; two Matt approvals — 16 September 2026, ~00:35 UTC

**Matt's approvals (00:32, from his phone):** (1) EDU-HEADER-BRAND — *accept the taller phone header*: "MADE BY MATT" on one line plus "Learn • Build • Explore" beneath wraps the bar one extra row at 320/390 (~129 → ~179 px); nothing else changes, Saved/search/Menu untouched. (2) EDU-TRY-LESSON — *add the missing Lessons-side catalogue entries for Friction and Diffusion first* (companion pack rows, duration, reference) rather than shipping a single static Sugar card. Both recorded; both bind EDU-D3.

**Lane D's first CI run went red on two checks, and the second was a real defect, not a records nit.** `git diff --check` redded trailing whitespace on all six lessons. `verify_definitions.py` raised *"Continuation destination or title differs from the actual lesson"* on two of them — and on investigation the cause was true of **all six**: the return-week builds carried from `claude/rw1-w8-return-week` had lost the authored `<script type="application/json" id="lesson-config">` block that every Science lesson on main carries and that `build_download_pack.lesson_label()` reads to name a lesson. Measured: `lesson_label()` returned **`None` for BUILD W8B** and **`'Your task · 1 of 2'` for the other five** (their only `<h1>` is a per-slide worksheet label, not a title). Only two were continuation targets and so gate-checked; the other four would have shipped mis-named in the offline pack portal and nobody would have known. Fix: `tools/rw1/restore_lesson_config.py` copies each lesson's own block verbatim from the review base (it invents no title; the committed continuation titles already match the base block exactly, which is the evidence this is the right repair), strips the whitespace, is idempotent, and self-tests five controls including the wrong-name-before / right-name-after pair. All three pathway transactions re-admitted; boundary PASS 6/0; cross-estate PASS; `git diff --check` 0.

**Carrier corrected.** All seven candidate digests moved with the fix (W8B `b67be20d → 492a4c28`), so carrier `429dd4ec` — minted from the pre-fix build — was stale and would have failed the publication. Carrier **`dd9d228a`** supersedes it; rollbacks unchanged; both builds verify green; 0 of 320 rows changed. **Lesson for the method:** mint the carrier only after CI is green on the Lessons head, not in parallel with it.

**Recorded, not fixed here (Matt list):** the return-week chassis has no lesson-title `<h1>` (W8B has none at all; the other five have nine per-slide task `<h1>`s). Main's versions had three. A lesson-title heading is a §25 structural expectation and a deliberate follow-up, not a late-night restructure of six decks.

**#543** is closed explicitly once #545 merges: its head sits outside the stack, differing only in pin and generated files re-derived here.

## 23. Lane D: two more chassis defects in the return-week builds, measured in Chromium and fixed — 16 September 2026, ~01:20 UTC

The corrected head `e685b556` cleared `verify_definitions` and `git diff --check`, and CI then reached the next gate, *Extracted lesson packs work offline* (`tools/downloads/offline_pack_browser.cjs`): **9 of 12 packs, 4 failures**. Reproduced locally with the estate's own tooling (Playwright on the pre-installed Chromium), then diagnosed by measurement rather than by reading:

1. **The floating guidance button sat on top of Next.** Every Science lesson carries the same N6M guidance shell (byte-identical block on main and in the return-week builds). Its `boot()` docks the button into `.controls .left` when that host exists — `if(host) b.classList.add("n6m-guide-docked")` — and otherwise appends a *fixed* button to `document.body` at the bottom-right corner. The return-week `.controls` bar (Previous · TA Brief · Question · Next) has no `.left` group, so the button floated, and `document.elementFromPoint` at Next's centre returned the guide button at **both 1280 and 390**. That is the exact defect the checker's own comment records ("Guidance covering Next"). Fix: `tools/rw1/dock_guide.py` inserts `<div class="left"></div>` as the bar's first child so the shell docks the button where the canon puts it. No script changes.

2. **The stage timer sat on top of "← Lessons".** `#auto-timer` (fixed at 14,10, 200×48, z-index 6000) covers the N6 nav shell's home link (8,6, 73×19) on all six lessons; the link cannot be clicked. Sixty lessons on main carry both widgets and avoid it with the `mbm-nav1` variant (home link fixed top-right), but that would collide with the return-week `.review-top` nav, also top-right. Fix: `tools/rw1/clear_home_link.py` moves the timer to `left:96px`. Measured after: link centre resolves to the link, the timer's own toggle still resolves to itself, no overlap with `.review-top` at either width.

Both tools are idempotent with planted-control self-tests. Full offline check after both: **PASS, 12/12 packs, 15 representatives, 94 guidance-navigation rows, 0 failures.** Only markup insertions (one empty `div`, one `style` rule); no pupil text moved, so the reading bands stand. All three pathway transactions re-admitted.

**Recorded, not fixed (Matt list, with entry 22's missing lesson-title `<h1>`):** the return-week toolchain on `claude/rw1-w8-return-week` produced lessons that drop the authored `lesson-config`, float the guidance shell and place the timer over the home link. Three chassis divergences from the estate's canon in one build; the toolchain, not just its six outputs, wants a look before the next return week is cut from it.

## 24. Lane D SERVED; companions carried; two catalogue defects found by CI and fixed; EDU-D3 built and measured — 16 September 2026, ~01:20 UTC

**Lane D closed.** #545 merged as `e1a05dce9c21db3648f9862884f8b96b9fc61d1e` (18 check runs green on 2d0f254b), publication run 35039428672 green, github-pages deployment 6471168152 `success` at 00:27 UTC with environment URL `https://madebymatt.uk/Lessons/` — **SERVED (platform-recorded; edge propagation unmeasured)**. #543 and #544 stayed open because their heads sit outside the merged stack; both closed with a comment naming the merge commit. Task #10 done.

**Companions (#551, CX2 §5.3 prerequisite).** Merged main at e1a05dce (`5085297c`), took main's generated files and re-derived the two entries with `tools/ux2/add_edu_q1_companions.py`; boundary PASS, cross-estate PASS, whitespace 0. Carrier **`40339776460347d0018569d598c45d0dd1a93497`** admits the six catalogue/discovery outputs the rows move as [served, candidate] (served oracle = full build of main e1a05dce at the carrier builder, 4044/4044 admitted; candidate 5085297c); pin moved (`370e4c8d`). CI then found **two real defects, both reproduced locally before fixing (`03a50119`)**:

1. `check_display_titles.cjs` and `build_display_titles.py` carried a typed 116 pairs / 232 entries; the manifest now has 118 / 236. Both derive the counts from `data/companion-packs.json` now.
2. **The Diffusion pack sat in the hub's "All" tab, not LAUNCH.** `hub.js tierOf()` read the pathway from the directory case-sensitively (`/Launch/`), the file-name prefix or the title prefix; `Science_Teesside/Teaching_Packs/LAUNCH/Week_4/W4L1_Diffusion.pptx` matched none. The hub browser gate's first pack chip therefore sat in a collapsed accordion and timed out (`hub_gates.mjs:210`). The directory test is now case-insensitive; a census over all 1077 catalogue rows shows exactly one row changes tier. Local `hub_gates.mjs --red-proof` PASS after the fix; catalogue DOM and static checks PASS.

3. **The chip-count gate keeps its own copy of `tierOf()`** (`tools/verify_lessons_chips.mjs`), so after the hub fix CI showed science/LAUNCH at 120 rendered vs 119 expected: the copy still expected the pack under "All". Aligned (`eb107c38`); 96/96 limbs and the red proof pass locally against the assembled mount. `hub_gates.mjs` itself passed 303/303 in CI on the hub-fixed head.

Because `hub.js` is a published file, the carrier is re-minted: **`7491329d8533b7edf6d32cfaa13a065c28af3f41`** ([served, candidate] for `assets/catalogue/hub.js` beside the six catalogue outputs); pin moved to it (`b2b876c1`). **#551 merged as `776cfbd1786981b383abac7e76a7490d9e21cc98`** with all 18 check runs green on `eb107c38`; publication run 35042882450 green (admission, package and deploy steps all `success`), github-pages deployment 6471716781 `success` at 01:16 UTC with environment URL `https://madebymatt.uk/Lessons/` — **SERVED (platform-recorded; edge propagation unmeasured)**. The Friction and Diffusion companion packs now sit on the catalogue beside Sugar, so the homepage rotation can bind all three.

**EDU-D3 (Site, branch `claude/edu-d3` off main c63149e) — built, measured and gated locally; Site PR #383 open (`424c3a9e` + `e3a09c49`), Lessons pin moved to `776cfbd1`; registry minted from full builds (32 pages moved, 4 files added, 29 superseded digests retired, 3,722 education-lessons rows re-derived); final build green against it, publications check PASS.**
- Site-main builder faithfulness control: full build of Site main at its pins (Lessons 2c33266b, Apps 924ab986) reproduces **3505/3505** admitted digests (0 problems); of the 180 two-digest rows, the live digest is the second in 178 (the first in 2).
- **EDU-HEADER-BRAND:** `tools/chrome/header.html` published-education fragment carries `<small>Learn • Build • Explore</small>` beneath the wordmark; `complete_chrome` censuses one header tagline (inside the brand lock-up only) and one footer tagline; `shared-navigation.css` puts the wordmark on one line (nowrap) and, on phones, gives the brand its own row above Saved/search/Menu (D1: taller header accepted). Gates moved: `check_chrome_tokens.cjs` (expected census follows the unified header; two red proofs: a stray header tagline and a removed brand tagline), `check_shared_navigation.cjs` (one brand tagline, text, one line each, ≥12px, no overflow), `check_ux2_education.cjs` (twice per route), `tools/test_published_site.py` (32 tests, new census test). Measured on the local mount: wordmark 1 line and tagline 1 line at 12px on every route at 320/390/768/1280 and at 150% text; header 175.4px at 320 and 178.8px at 390 (baseline Site main: 175.4 / 129.2), 127.6px from 768.
- **EDU-HERO:** both approved sources located by full SHA-256 (homepage `c6c1b9e8…b248`, Teachers `4cdfedd8…e77e`). `prepare_education_hero.py` cuts three crops (PIL `Image.crop` only, JPEG q85): home 600×238 (33,373 B, `23f5a048…6a12`), home-phone 320×180 (15,982 B, `20e0778f…0ac61`), teachers 430×385 (40,164 B, `c33d5093…1ecb`); `education-hero.json` records source digest, crop box, size, bytes, served digest and the refinements each crop does and does not satisfy; `--check` re-proves the committed files. The renderer refuses artwork whose bytes differ from the manifest; the front-door gate proves source digest, served digest, byte count, empty alt, the phone crop below 768px and nothing overlapping the picture. D5 decided: `main img:not([data-preview-source])` becomes `…:not([data-hero-source])` on both gates — the intent (no invented pictures) is kept by binding every picture to an approved source by digest.
- **EDU-TRY-LESSON:** `homepage-feature.json` is schema 2, bound by `bind_homepage_features.py` from the Lessons checkout: lesson route and digest from the pack row's `companionOf`, title and reference from `display-titles.json`, description from the lesson's catalogue row (Sugar has no lesson row, so its first "I can…" outcome sentence, recorded as such), pathway/subject/term from `data/companion-packs.json`, duration evidenced in the lesson text, previews at their reviewed PDF digests, pack route `/Lessons/pack.html?id=…`. A stale binding is a build error, never a shrunk card; a catalogue that carries none of the declared packs renders no feature and the gate reports it. `try-a-lesson.js`: 8-second rotation, Previous/Next/Pause–Play, visible "n of 3", stop on focus or manual selection (resume only by Play), pause on hover/hidden page, reduced motion starts stopped, no live region, inactive slides hidden; without JavaScript the Sugar card renders linked. Day and Night stands down from the homepage feature (it is not an EDU-Q1 identity).
- **Local gate findings (rabbit):** (a) hero-slot rotation was hidden by a higher-specificity rule (fixed: `.fd-pack.fd-try.fd-hero-pack`); (b) on phones both rotation instances were visible — the new display rule outranked the hero-slot hiding (fixed); (c) rotation buttons overflowed at 150% text (controls now wrap); (d) the first brand red proof accepted a removed tagline (expected census now follows the unified header); (e) `review` scoped inside the route loop; (f) Appendix A still listed "Find teaching resources →". Pre-existing overflow (unchanged from Site main): hub-links "Resources" 72>69px at 320 on every route; three ellipsised half-term rows; subject tiles at 390×150%.
- **Site separation check re-frozen by diff:** the Site-main check pins the retained usage-registry rows by digest, and that digest moves with the Lessons pin. A full build at the current pin (2c33266b) reproduces the committed baseline `d2439c61…` exactly (926 rows); the build at the new pin gives `ec7dc075…` (977 rows: 51 Science_Teesside lesson records added, 0 removed, 1 field changed — the Sugar title after Lessons #547), the same digest the carrier already records for that state. The 22 reviewed Science download rows whose titles carry KB sizes were refreshed by the existing sync tool.
- **Decisions under "decide and continue":** D1 accepted (taller phone header); D2 = (ii) the Lessons-served header keeps the old lock-up until the carrier builder is refreshed — a visible seam between `/` and `/Lessons/`, recorded for MAINT-1 (evidence: the carrier builder has no `complete_chrome`); D3 = 12px tagline; D4 = companions first (Matt); D5 = provenance-bound picture gate; hero refinement 2 (prism off the owl's face) cannot be met by a crop — recorded as a deviation with screenshots; the crops are 600/320px wide from a 1536px mock-up (about 1× on a phone).

**Adversarial audit (`wf_cdc84de5-a06`), sixth dimension in (75 agents).** 8 findings survived, 15 refuted. All eight describe states since superseded: #547's and #543's pre-carrier pins and the sparse-build digest (both replaced by carriers minted from full builds, entries 17–23; both merged and served), the `--allow-sparse` reproduction gap (entry 15, method withdrawn), an entry-7 byte-check wording error and a stale #538 body line (history, not served bytes). Two hygiene notes carried, not fixed: the S3 companion evidence table is not expressed in bytes and its headline row was measured on the #538 branch; every recorded GLV3 self-test control count is one higher than the instrument reports.

**Matt list additions.** Open on the phone after the Site publication: `/` (header, owl, Try a lesson: Sugar → Friction → Diffusion), `/for/teachers/` (classroom art), `/Lessons/` (the seam: old header lock-up until MAINT-1). Prism still on the owl's face; phone hero at ~1× — a higher-resolution render of the approved artwork would sharpen it.

Evidence for this entry: `docs/edu-d3/evidence/` (screenshots at 320/390/1280 and at 150% text, `measurements.json`, `site-usage-registry-refreeze.json`).
