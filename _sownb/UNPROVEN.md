# ORDER RSH — withdrawn / unproven rows

Rows below are not asserted, gated or built. Counts use `grep -n -F -o` over the four §P1 references.

- `.box.ido` — count `0`. Literal selector is absent from all four named references. Composite markup such as class="box ido" does not prove that exact contract selector.
- `.box.wedo` — count `0`. Literal selector is absent from all four named references. Composite markup such as class="box wedo" does not prove that exact contract selector.

`.prog` is not on this list: measurement found it in both Science references. It is retained only as a Science-family row and is not asserted for either ASDAN family.

## ORDER FEB — inherited metric rows withdrawn from production

The existing contract rows remain byte-for-byte present for provenance, but are excluded from the FEB production denominator because their own named live donors do not meet their recorded `reference_count`:

- `family.grow-asdan.control-count` — recorded `8`, donor DOM measures `6` control-bar buttons.
- `family.grow-science.control-count` — recorded `11`, donor DOM measures `10` control-bar buttons.
- `family.launch-asdan.control-count` — recorded `7`, donor DOM measures `6` control-bar buttons.
- `family.launch-science.control-count` — recorded `11`, donor DOM measures `10` control-bar buttons.

The rows' stored grep proofs count occurrences of the CSS string `.controls`; they do not prove a rendered control-bar button count. Trusted pointer and keyboard installation remains the binding production measurement.
