#!/usr/bin/env node
"use strict";
const fs=require("node:fs"),path=require("node:path");const{pathToFileURL}=require("node:url");
const modules=process.env.CODEX_PRIMARY_RUNTIME_NODE_MODULES,binary=process.env.RSH_CHROMIUM_PATH;if(!modules||!binary)throw new Error("proved runtime and Chromium required");
const{chromium}=require(path.join(modules,"playwright"));const ROOT=path.resolve(__dirname,"../../..");
(async()=>{const rel=process.argv[2],output=process.argv[3];if(!rel||!output)throw new Error("usage: render_pdf_w2.js HTML OUTPUT");const file=path.resolve(ROOT,rel),out=path.resolve(output);fs.mkdirSync(path.dirname(out),{recursive:true});const browser=await chromium.launch({executablePath:binary,headless:true,args:["--no-sandbox","--disable-dev-shm-usage"]});try{const page=await browser.newPage({viewport:{width:794,height:1123},colorScheme:"light"});await page.goto(pathToFileURL(file).href,{waitUntil:"load"});await page.emulateMedia({media:"print",colorScheme:"light"});await page.addStyleTag({content:"@media print{html,body{color-scheme:light!important;background:#fff!important;color:#111!important}}"});await page.pdf({path:out,format:"A4",printBackground:true,preferCSSPageSize:false,margin:{top:"0mm",right:"0mm",bottom:"0mm",left:"0mm"}});console.log(JSON.stringify({status:"PASS",html:rel,pdf:out,bytes:fs.statSync(out).size,chromiumVersion:await browser.version()},null,2));}finally{await browser.close();}})().catch(error=>{console.error(error.stack||String(error));process.exitCode=1});
