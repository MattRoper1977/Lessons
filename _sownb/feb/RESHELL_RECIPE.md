# FEB family-parameterised shell recipe

This recipe supersedes the family-agnostic reading of the original W7A recipe. It does not rewrite the protected RSH-3 recipe.

1. Bind every candidate to its own named live lesson donor and front-door donor at anchor 4916fe9511e24e1f5541e7454f3d2e9d4bac5f75. A missing donor holds that family alone.
2. Install only the custom-property names and values measured in that family and frozen in `_sownb/G19_TOKEN_OWNERSHIP_FEB.json`. Never translate a token name between families.
3. Existing RSH-3 bindings remain authoritative for GROW Science, GROW ASDAN, LAUNCH ASDAN and LAUNCH Science.
4. BUILD Science owns `--grow`, `--navy`, `--text`, `--muted`, `--bg`, `--lo`, `--purple`, `--pink`, `--amber`, `--blue`, `--green`, `--softblue`, `--softamber`, `--softgreen` and `--line` with the measured donor values.
5. BUILD Humanities owns `--ink`, `--steel`, `--steel-dark`, `--rust`, `--rust-dark`, `--teal`, `--teal-dark`, `--gold`, `--cream`, `--line`, `--muted`, `--green`, `--paper` and `--focus` with the measured donor values.
6. BUILD ASDAN owns `--ink`, `--paper`, `--soft`, `--line`, `--muted`, `--gold`, `--green`, `--slot`, `--slot-pale` and `--danger` with the measured donor values.
7. GROW Humanities owns `--bg`, `--panel`, `--panel2`, `--text`, `--muted`, `--steel`, `--oxide`, `--teal`, `--gold`, `--violet`, `--line`, `--paper`, `--ink` and `--danger` with the measured donor values.
8. LAUNCH Humanities owns the same measured name set except `--danger`, which its donor does not declare.
9. Install the exact estate splash and guide blocks with both n6-nav1 marker pairs, print-hidden splash, hidden-by-default staff guidance and `display:revert!important` reveal.
10. Install the shared overlay route, nine-stage 40-minute shape, complete Lundy strip and the family's proved furniture dialect. Shared contract rows still bind universal furniture that an older family donor does not yet carry.
11. Paint only the tier ladder measured in the family donor. All five newly extended families use Supported / Standard / Stretch.
12. Keep the exact 566-byte learner-confirmation block inside `.print-pack`; same-family confirmation rows without a literal donor proof remain in `UNPROVEN_FEB.md` while the shared rows bind.
13. Trace every teaching outcome to a named workbook cell. Never backdate, reconstruct a quotation, stage an event or convert adult wording into the learner's voice.
14. Freeze the contract denominator and token configuration before candidate measurement. Drift stops the affected surface.
