# ORDER LW-1 — LEARNING-WALK PRIORITIES (headteacher, Sept 2026)
Received 2026-09-22. Runs AFTER Autumn 2 batch 1. Standard limits and landing route.

## M — MEASURE (read-only, per current lesson, both hubs)
- **M1 Activity census, row family 50** — from the RENDERED DOM, count interactive
  elements by kind:
    retrieval/quiz (arrival routes, reveal)
    matching/sorting/sequencing (drag/sort tasks)
    scenario/decision (branching or "choose" tasks)
    think-pair-share / structured talk prompts
    practical/demonstration stages
    questioning (hinge/check stages)
    multimedia (embedded video/model/diagram)
  Report per lesson: kinds present, kinds absent, and
  "worksheet dependency" = printable task pages / interactive kinds.
- **M2 Stretch reality, row 51** — the Stretch route differs from Standard in DEMAND
  (question text and answer guidance NOT a superset of Standard's). Measured, not assumed.
- **M3 EHCP hook, row 52** — does the deck or its SoW carry any outcome-area tag
  (communication / cognition / SEMH / sensory)? Expect none; report.
- **M4 QR presence, row 53** — expect none.
- Report as ONE table, seven priorities as columns, CONFORMS / PARTIAL / GAP per lesson.
  Exemplar gaps to Matt.

## R — RULINGS (Claude's to carry out)
- **R1 Priority 6** — every print pack and exit ticket gains a QR code encoding the
  lesson's SERVED URL, generated by a writer FROM THE ROUTE (never typed), inline SVG,
  <=1 KB, alt text = the URL. Added at the PRINT-FURNITURE level so it applies to every
  conforming lesson in one PR family.
- **R2 Priority 2** — an EHCP outcome-area tag line on the TA brief, derived from the
  SoW's area-of-need column IF IT EXISTS; if it does not, EXEMPLAR GAP for Matt to add at
  scheme level -- not authored per lesson.
- **R3 Priority 7** — NO lesson is rewritten by a tool. The census tells Matt which
  lessons are worksheet-heavy; those go to the GPT science/humanities brief as
  "add two interactive kinds" items with the SoW objective, per lesson.
- **R4 Priority 4** — a Stretch route that fails row 51 is a named gap for the same brief.
- **R5** — the hub gains a "Learning-walk ready" pill derived from rows 50-53 alongside
  CONFORMS, so Matt can point to it.

## PROOF
rows 50-53 red-proved; QR decodes to the served URL in Chromium; served proof per batch.
CLOSE with the seven-column table and the list for GPT.
