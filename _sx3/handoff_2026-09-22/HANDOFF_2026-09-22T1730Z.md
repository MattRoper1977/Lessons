# HANDOFF — 2026-09-22T17:30Z

Written at Matt's request when execution moved to a fresh session. Everything
below is **derived at the moment of writing** — `git rev-parse`, the GitHub API,
and the tools' own output — not recalled.

Artefacts referenced by `$SP/...` in earlier conversation are **copied into
`artefacts/` beside this file**, because the scratchpad does not survive the
container.

---

## 1. Mains, and the last closing publication per repo

| repo | `origin/main` | last closing publication | verdict |
|---|---|---|---|
| **Lessons** `MattRoper1977/Lessons` | `91ed798075221cb15b0462dacbe57d59c9006db5` | run **`35756213748`** (`education-pages.yml`) | build `106842521996` SUCCESS · deploy `106846552337` SUCCESS |
| **Apps** `MattRoper1977/Matt-s-Apps-` | `2e6303252a1b82159d68309de2234b3f43bbcb9f` | run **`35755635089`** (`education-pages.yml`) | SUCCESS |
| **Site** `MattRoper1977/mattroper1977.github.io` | `acb7bfa9bb525ecd3dbffee2c68c208719d2888e` | run **`35755898332`** (`mbm-deployment-provenance.yml`) | SUCCESS |

Main subjects:

- Lessons — `Pure carrier: move the publisher pin to Site main acb7bfa9 (#648)`
- Apps — `Apps gate copy: carry the carrier move to Site main acb7bfa9 (#170)`
- Site — `Step 3: discharge the EQUAL pins, and raise the admission job's clock (config only) (#432)`

Pin state at these heads:

- Lessons carrier `education-pages.yml` → `uses:@acb7bfa9…` and `builder_ref: acb7bfa9…`
- Apps carrier → `education-publication.yml@08d74766…` (separate pin; **not** the same as the Lessons carrier — do not "fix" this without a ruling)
- Gate copy `tools/verify_cross_estate_unification.py`, both estates: **`ba4dc229bede`** on main (derived from `git show origin/main:` in each repo at 17:32Z); `c1da1041463a` on the open naming pair. `c20ec5530b60` was its value *before* the carrier pair #170/#648 — do not use that figure
- Four EQUAL pins all at Lessons `b5b87361` / Apps `86933c99`, discharged at 0 commits behind

**The Summer 1 responsive fold is CLOSED.** Source-side served proof at
`91ed7980`: 21/21 parseable whole documents, 21/21 `meta viewport
width=device-width`, 21/21 `@media … max-width: 600px`, asserted by DOM over the
whole document rather than by grep.

---

## 2. Open PRs and unpushed branches

### Open and ACTIVE — this is the live work

| repo | PR | head | carries | CI at 17:30Z | waits on |
|---|---|---|---|---|---|
| Apps | [#171](https://github.com/MattRoper1977/Matt-s-Apps-/pull/171) | `c80a072f` | gate copy for the STOP-C1/C3 catalogue re-pin | **GREEN** — 5 success, 4 skipped | merge (it goes FIRST, per correction #18 / L31) |
| Lessons | [#649](https://github.com/MattRoper1977/Lessons/pull/649) | `bd4aa84c` | the science stage-identity check change + STOP-C3 D1–D3 + catalogue re-pin + splitter admission | 10 success, 1 skipped, **4 in_progress** (`browser-matrix` 106856851825, `Original Science navigation` 106856851311, `keyboard-reachable` 106856851123, `Chromium 390px` 106856848732) | its own CI, then merge AFTER Apps #171 |

`claude/sci-stage-identity` @ `bd4aa84c` is **pushed and clean**; `claude/sci-stage-identity-apps` @ `c80a072f` is **pushed and clean**. No half-pushed branch anywhere; all three clones report `dirty=0`.

### Open but NOT active

- **Lessons** — #609 (PACK-1R Hum Sum1 W1), #596 (SX3 PASS 7 CLOSE, docs, merge on acceptance), #560, #497, #465, #456, #118 `[PARKED — DO NOT MERGE, RULED]`, #116 `[REFERENCE DIFF — DO NOT MERGE]`, #45 `HELD`, #43 `HELD on print check`.
- **Apps** — #4 (draft, TW-1 QA retention, Matt's to merge; `no-ff, never squash`).
- **Site** — #382, #381, #380, #371, #370, #368, #366, #363, #362, #361, #358 are all **immutable "do not merge into Site main" publisher carriers** from the codex era; #291 is `HELD — HC3 touch instrument cannot zoom its unblocked positive control`. **None of these is yours to merge.**

### Local branches

Each clone carries ~30–90 stale local branches. Almost all are squash-merge
artefacts: they show `ahead=N` against `origin/main` only because the merge
squashed them. The only local branch holding work that is **not** on a remote and
**not** already merged is:

- `/home/user/lessons` → `keep/sci-rebased` @ `404abb8c` — a safety copy of the
  pre-merge rebase of the naming branch, taken when a force-push would have been
  required. **Superseded**; its tree is identical to what `claude/sci-stage-identity`
  carries. Safe to ignore; not deleted, per the standing rule.

---

## 3. Ruled-but-not-done, in execution order, with the ruling quoted

### 3.1 — STOP-C3 D1–D3 → the naming PR  ✅ IMPLEMENTED, ⏳ NOT MERGED

> **D1 SCOPE BY IDENTITY CHANNEL:** rows 2/14/45 gate a deck on the naming channel
> the deck actually declares — Humanities decks on the STAGE_NAMES table; science
> decks generated by build_html.py (the eyebrow channel) on the 16-phrase
> vocabulary. A deck resolving to NO channel is RED "no identity channel" (never a
> pass). The 58 other science decks that mismatch are out of this PR's scope by
> that rule and are LISTED by name with their channel result; they are re-named
> when their PASS C batch transplants them, not now. Red proofs: a Humanities deck
> → table channel, 0 change; an SX3 deck → eyebrow channel, expectation as tabled;
> a deck with both channels absent → RED.
>
> **D2** Row 47 PATHWAY_SEGMENT derived from `<html class="pathway-…">`, not
> hard-coded science. Red proof: a Humanities deck with a panel planted on its
> title stage → RED (today it passes).
>
> **D3** Ordinal bound: at most two of each modelling kind; a third bare "I do"/"We
> do" → RED "unbounded modelling". Red proof planted.

All three are implemented and red-proved on `bd4aa84c`. Measured:

| population | decks | stages | names changed vs main | channel | rows 2/14 |
|---|---:|---:|---:|---|---|
| Humanities | 230 | 2123 | **0** | 230 table | **230 OK** |
| `verify_loop` run population | 91 | 827 | **0** | 91 table | **91 OK** |
| the ruled 31 | 31 | 290 | 31 | 31 eyebrow | **31 OK** → **208** |
| Science (all) | 200 | 2201 | 89 | 89 eyebrow / 111 table | 149 OK, **51 mismatch** |

Self-tests: `verify_loop --self-test` **41 pure controls, 41 PASS**;
`split_arrival_stage --self-test` **29 PASS**; unification `--self-test`
positive-control PASS (3 detected errors).

**Two corrections to figures in the ruling and in my earlier reports:**

1. It is **51 decks, not 58.** The 58 was `89 eyebrow-channel − 31`, taken before
   the rule tested the table channel first. The ruling carried my number forward;
   the corrected list is `artefacts/SCI_51.md`.
2. **No deck resolves to "no channel"** — not the 19 I reported. That 19 came from
   a throwaway helper lacking `table_stage_name`'s `KIND_STAGE.get(kind, kind)`
   fallback. The "no identity channel" RED is therefore proved by **synthetic
   control only**, not by a real deck.

### 3.2 — regenerate `PASSC_ROW45_DERIVED.json`  ⏳ NOT DONE

> "…merge on green → regenerate PASSC_ROW45_DERIVED.json → Autumn 2 batch 1"

Blocked on 3.1 merging. The current file is copied to
`artefacts/PASSC_ROW45_DERIVED.json`; it must be regenerated **after** the naming
PR is on main so the row-45 column is usable.

### 3.3 — Science PASS C Autumn 2, the SX3 31 in batches ≤12  ⏳ NOT STARTED

> "Science PASS C Autumn 2: the SX3 31 in batches ≤12 (P1 shape, derived row-45,
> four term-week forms, evidence re-stamp, pack SHA256SUMS via transaction). Hub
> CONFORMS flips per batch."

Also pending from the earlier ruling: **the three merged decks must be split
first** —
`SCI_L_W9_Copy_separate_divide_Classic`,
`SCI_L_W12_Zoom_into_genetic_information_Classic`,
`SCI_L_W13L2_Punnett_Square_Explore`.
All three are verified to split 8→9 with the timer total unchanged and
`build_catalogue`'s own title-slide fallback reading `['Aut2']` **before and
after**. The splitter is ready; the split has **not** been applied.

### 3.4 — B1 Spring 1 (10), then Spring 2, Summer 1, Aut1 v3 sets  ⏳ NOT STARTED

### 3.5 — HUM-T batch-1 P1 re-cut (BUILD W1–W8)  ⏳ NOT STARTED

### 3.6 — ORDER LW-1  ⏳ NOT STARTED (queued behind Autumn 2 batch 1)

Full text at `artefacts/ORDER_LW1.md`. Rows 50–53, QR writer R1, census; R2/R3/R4
become a list for Matt/GPT, **not edits**.

One thing worth knowing before it starts: **M1 wants the rendered DOM, and
rendered-DOM work does not need egress** — Chromium is local and the publication
mounts locally. The only LW-1 item that would need the network is a live-served
check; the QR proof decodes a locally generated SVG.

### 3.7 — Next-order, only when everything above is closed

- **Split the browser mount into its own job.** Now evidenced: Site #432's
  admission job ran **29m02s**, of which step 24 `Mount and exercise the actual
  education publication` was **20m43s — 71%**. Admission (step 14) and the mount
  should return separate verdicts.
- `find_pins` follow-ups; evidence-body re-derivation (56).

---

## 4. Held by name — never forced, listed here with the file that records each

The authoritative record is **`_sx3/HELD.md`** (202 lines) unless stated otherwise.

| set | count | where recorded |
|---|---:|---|
| **A** — held under standing Ruling 4: a second generation, no ruling | 10 | `_sx3/HELD.md` §A (also `_sx3/RELEASE_LEDGER.md`, `_sx3/SHELF_RECLASSIFICATION.md`) |
| **B** — held on the Original Science stage contract | 10 | `_sx3/HELD.md` §B, incl. "Set A after ORDER SX3-M2 §3" and "Set B … 12→13 question withdrawn" |
| **C** — parked earlier, still parked | 15 | `_sx3/HELD.md` §C |
| **D** — the 5 GROW `_Do` decks carrying their `_Explore` partner's lesson | 5 | `_sx3/HELD.md` §D, with the per-deck table (`SCI_G_W9B`, `W10B`, `W11B`, `W12B`, `W13B`, each "identical" to its `_Explore` partner, 48,766–58,217 chars) |
| **15 LAUNCH print** | 15 | `_sx3/HELD.md`; also `_sx3/CHASSIS_CONTRACT.md`, `tools/catalogue/TERM_AND_STYLE_EVIDENCE.json` |
| **5 week-token disagreements** | 5 | `_sx3/RELEASE_LEDGER.md` / `tools/catalogue/TERM_REVIEW.md` — **not** in `HELD.md`; grep for the term-week forms |
| **2 case-study decks** | 2 | `_sx3/RELEASE_LEDGER.md` — **not** in `HELD.md` |
| **51 unscoped science decks** (was "58") | 51 | **`artefacts/SCI_51.md`**, beside this file — name, channel, declared/resolved for modelling and title |

`HELD.md` records the union of A+B+D as **25**, with A∩B, A∩D, B∩D all empty, and
a landing-set table showing 36 measured before the D holds → **31 land, 44 held**.

---

## 5. Standing rules and corrections added this session

| id | one line | where it lives |
|---|---|---|
| **L38** | a carrier that moves a published file is not a carrier | `_sx3/SX3_PASSES_LEDGER.md`; enforced by `tools/hum/check_carrier_pure.py` |
| **the moving-set rule** | the moving set is the content diff **plus every derived served artefact**; the window is cut from the tree **after** the derivations run, never from the diff | Lessons #647 commit body |
| **the carrier-last rule** | a carrier must point at the Site SHA admitting **every** mover, derived artefacts included — so the carrier bump is the **last** edit on the Lessons branch, after every window it depends on has merged | Lessons #647 and #648 commit bodies |
| **the pending-then-land fold** | any pair whose diff intersects the served set lands as: window admits `[main-today, pending]` → content+carrier → small EQUAL re-pin window → pure carrier | Site #430 PR body; used for #430→#432→#648 |
| **correction #18** | open the Apps companion PR **before** the Lessons PR | every companion PR body this session |
| **correction #33** | the served-set intersection is run **BEFORE** merge, not after | Site #432, Lessons #648, Apps #170/#171 bodies |
| **correction #37** | a wait is only a wait if the turn **holds** for its notification | this session's conduct |
| **STOP-RESP-1 (c) route** | a tool the standalone/offline boundary refuses is admitted by `REVIEWED_PATHS` **with its derived trigger pair** — a pin, a tightening, never an exemption | `tools/catalogue/pin_catalogue_contract.py` (the `refresh_pack_checksums.py` block, and now the `split_arrival_stage.py` block beside it) |
| **the ledger-evidence rule** | every ledger entry's run ids, counts and code references are **re-derived from primary sources** before the entry is committed | Lessons #646 commit body |
| **correction: admission clock is not monotonic** | 24m09s → 31m47s → 32m05s → **29m02s**; the raise to 60 is headroom, not a response to a monotonic trend | Lessons #648 PR body |

Corrections made to my **own** reported figures this session, all stated where
they were made: 51 not 58 unscoped decks; 0 not 19 no-channel decks; 27 not 26
pure controls; 290 stages confirmed as 268+20+1+1.

---

## 6. Artefacts copied beside this file

`_sx3/handoff_2026-09-22/artefacts/` — all previously `$SP/…`, now durable:

| file | what it is |
|---|---|
| `STOP_C3.md` | the three defects the adversarial review found, as written up before the ruling |
| `SCI_51.md` | **the 51 unscoped science decks**, by name, with channel and declared/resolved counts |
| `ORDER_LW1.md` | ORDER LW-1 verbatim |
| `STOP_C1.md`, `STOP_C1_DERIVATION.md` | the science stage-identity derivation, incl. the `data-type` / eyebrow-class conflation tables |
| `RULING2_TITLE_CONTROL.md`, `RULING3_VACUOUS.md` | the title-control and vacuity rulings as worked |
| `PASSC_ROW45_DERIVED.json` | the file to **regenerate** after the naming PR merges |
| `RESP_21_URLS.json` | the 21 Summer 1 responsive page URLs — **needed for the owed 390 px proof** |
| `SERVED_PROOF_su1.md` | the Summer 1 served-proof record |
| `ADMISSION_CEILING.md` | the admission-job duration trend |
| `EGRESS_FINDING.md` | the egress denial, recorded as a container limit and not a pass |
| `STOP_RESP3_derived_movers.md` | the derived-mover finding that produced the 28th mover |
| `the31.txt` | the SX3 31 landed science decks |
| `declared_mismatch.json` | the full declared-vs-resolved measurement across both subjects |

---

## 7. Owed — pending Matt, not pending code

1. **The 390 px served proof on the 21 Summer 1 Humanities pages.** Container
   egress to `mattroper1977.github.io:443` is refused — `HTTP 000`, 3/3, re-tested
   at 16:25Z and again at 17:0xZ; it worked at 12:10Z the same day. **Recorded as a
   container limit, never as a pass.** The source-side DOM proof PASSES 21/21;
   the *served* 390 px check is Matt's to do on his phone. URLs in
   `artefacts/RESP_21_URLS.json`. Three to check:
   - `https://mattroper1977.github.io/Lessons/Humanities_Teesside/BUILD_W27-W39_2026-27/BUILD/Summer_1/W02/BUILD_SU1_W02_Pupil_Resources.html`
   - `https://mattroper1977.github.io/Lessons/Humanities_Teesside/GROW_W27-W39_2026-27/GROW/Summer_1/W01/GROW_SU1_W01_Pupil_Resources.html`
   - `https://mattroper1977.github.io/Lessons/Humanities_Teesside/LAUNCH_W27-W39_2026-27/LAUNCH/Summer_1/W01/LAUNCH_SU1_W01_Pupil_Resources.html`
2. **NOAA 2025 = 427.35 ppm — UNVERIFIED-BY-CLAUDE.** Recorded in
   `_passhumd5/CHECKS.md` (§ruling 4 v2, and the summary at line ~691). Nothing is
   edited until Matt replies `"NOAA ok"` or supplies a value. The authorised
   difference `R1-LAUNCH_SU1_W01-NOAA-SIX-YEARS` stands.
3. **`serve-witness.yml:31` P0 red** — a Lessons checkout with no `ref:`, no
   floating declaration, no `rev-parse` recording its resolved commit.
   Pre-existing on main, surfaced by the scan-ceiling removal in #429. It is
   **not** a CI gate (every reference to the lag control in `.github/workflows/`
   is inside a comment). **Raised for a ruling; not fixed** — no check is edited
   without one.
4. **24 shipped Humanities decks carry a pupil-response panel on the slide
   holding the lesson's own `<h1>`.** Confirmed under `origin/main`'s **own**
   code: row 14 FAILs on 24 of the 91 run-population decks with `bad=['title']`.
   Nothing runs row 14 over landed decks (`adapt` refuses to transplant twice), so
   it has gone unseen. Row 47 binds the same 24. **Pre-existing; raised, not
   changed.**
5. **`ux2-gates.yml` path filters exclude `Science_Teesside/**`** — raised
   earlier, unruled.
6. **60 pack `SHA256SUMS` entries naming unshipped files** — pre-existing, raised.
7. **STOP-X §4 SHELF** — writer gated on the 2026-09-06 audit; 77/129 stale, 48
   re-provable, 29 not. Matt's decision.
8. **ADDENDUM 2 to HUM-D5** — 12 Summer 1 lessons (BUILD + GROW); GROW input
   withdrawn.
9. **L51 ledger entry** — drafted (`artefacts/L51_draft.md`), **not landed**. Per
   the ledger-evidence rule it must be re-derived from primary sources before it
   is committed; the draft is a starting point, not evidence.

---

## 8. The first command the successor should run

```bash
# 1. Confirm the two open PRs and their CI, then merge Apps FIRST, Lessons second.
#    Both bodies state exactly what they carry; neither has been merged.
gh pr checks 171 --repo MattRoper1977/Matt-s-Apps-     # expect: green (was green at 17:30Z)
gh pr checks 649 --repo MattRoper1977/Lessons          # 4 jobs were still running at 17:30Z
```

(there is no `gh` in the remote container — use the GitHub MCP tools
`mcp__github__pull_request_read` with `method: "get_check_runs"`.)

Then, **in this order**:

1. Merge **Apps #171** on green — it must precede the Lessons PR (correction #18 / L31).
2. Merge **Lessons #649** on green. Read the publication it triggers **by run id**.
3. Regenerate `PASSC_ROW45_DERIVED.json` on the new main.
4. Split the three merged decks, then Autumn 2 batch 1 in batches ≤12.

**Before touching any deck-naming code**, read
`tools/hum/STAGE_IDENTITY_RULE.md` — STANDING INSTRUMENT RULE 5 forbids
`data-type` as an identity, and it is the rule this whole change implements.

**Environment note that will cost a successor an hour otherwise:** use
`/tmp/pubvenv/bin/python` (3.12 + lxml 6.0.1). The system `python3` is 3.11 and
**cannot parse this repo's nested-quote f-strings**. Clones live at
`/home/user/lessons`, `/home/user/matt-s-apps-`, `/home/user/mattroper1977.github.io`.
