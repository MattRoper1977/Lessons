# IN-FLIGHT — Matt's approved jobs, 5 August 2026

**Declared before any other write.** Base derived at run start: `origin/main` = `df18729`.

## What is approved, and what is not

| job | status |
|---|---|
| Constructed-source labels — 3 non-assessed LAUNCH humanities decks | **APPROVED as worded — applying** |
| TBC unit-code hides — 25 science witness sheets | **APPROVED — applying** |
| The D&T dust/timber set — 4 BUILD_DT decks | **NOT APPROVED. Never applied, and preserved intact.** |
| Record the code-scoping ruling (BUILD/GROW only on restore) | approved |
| Record the Mel routing, and render the D&T proposal in plain English for her | approved |
| Merge the display-artefact audit record | approved |

## The structure of the held proposal set, derived not assumed

All three proposals live as **diff files** on the held branch `claude/semh2-claims-accuracy`.
That branch touches **zero** lesson files directly — it carries text describing changes, not the
changes themselves. So the two approved sets are applied here as **ordinary authored commits by
this session**, gated per file, and the application is mine rather than a merge of someone else's
work. The held branch is left exactly as it is, which is also how the unapproved set survives.

## The unapproved set — where it lives and why it is not touched

`_semh2/proposed_dt_dust_ht.diff` on branch `claude/semh2-claims-accuracy`, targeting
`Build/Slideshows/BUILD_DT_W1_Workshop_Audit.html`, `BUILD_DT_W2_Blueprint.html`,
`BUILD_DT_W3_Core_Cut.html` and `BUILD_DT_W5_Finish.html`.

It is held on a health-and-safety sign-off that has not happened. **It must survive exactly as
proposed** — not deleted, not rebased away, not orphaned — because the person reviewing it needs
to read what was actually proposed, not a summary or a reconstruction. Nothing in this pass
merges, rewrites or rebases the branch it sits on.

## Frozen in this pass

- `Grow/Slideshows/GROW_HUM_W7_Write_The_Account.html` and
  `Launch/Slideshows/LAUNCH_HUM_W7_Source_Assessment.html` — 0 bytes. Neither approved diff
  targets either file; verified before applying, not after.
- `biology/`, `chemistry/`, `2 Physics 10/` — tree hashes unchanged.
- **Nothing hidden is restored.** The hides go on; nothing comes off. The restore waits on a
  person and is scoped BUILD/GROW only.
- **No printer hold is released.** No art merge, no banner removal, no large-print action.
