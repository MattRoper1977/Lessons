__pycache__/
*.pyc

# © ASDAN source instruments — NEVER commit (see _passpq/inputs/README.md).
# Facts are extracted with citations into _passpq/*.md; the sources stay local.
asdan-peq-specification*.pdf
asdan-qualifications-key-dates*.pdf
assessment-plan-blank-template*.docx
*asdanpeqspecification*.pdf
*personal-effectiveness*flyer*.pdf
_passpq/inputs/*.pdf
_passpq/inputs/*.docx

# Transient gate output. audit-output/ is ~15 MB of screenshots and JSON
# rewritten on every browser-audit run; _glv3/.contact_tmp/ is scratch.
audit-output/
_glv3/.contact_tmp/

# © ASDAN private inputs for PH-3 — never committed, never zipped.
_asdan_private/

# SCA-1 tool working outputs — regenerable scratch, never a record.
# (_sca1/PROTECTED_MANIFEST.base.txt and .now.txt ARE the records and stay tracked.)
_sca1/streams/
_sca1/PROTECTED_MANIFEST.txt
_sca1/food_census.json

# transient output of _sca1/tools/protected2.py - regenerated on every run
_sca1/PROTECTED_MANIFEST.WORKTREE.txt
_sca1/food_census.WORKTREE.json

# PEQ-YEAR-3: the owner's timetable workbooks are EVIDENCE, never redistributed.
# Their facts live in _passpq/TIMETABLE_2026-27.md with a source cell per row.
Timetables_2026_2027*.xlsx
Colour_coded_teacher_subject_tables*.xlsx
# Agent/CI installs must never enter the tree (playwright, jsqr, ...).
# NB: node_modules/playwright{,-core} symlinks from 4b1029e are already
# tracked; ignoring the directory does not untrack them.
node_modules/
