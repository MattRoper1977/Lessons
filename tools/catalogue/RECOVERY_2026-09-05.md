# Catalogue recovery boundary — 2026-09-05

This draft is recovered, not approved for publication.

The stopped sessions' catalogue changes were reconciled locally with the accepted
GROW and LAUNCH source tree. GROW PR #329 merged as
`976a8ca98b7a86a06ab76cb06c452e0f3971052e`; LAUNCH PR #327 merged as
`ca43634c4662f5ac9fa7793b75311930caba4d8f`. The source tree used for regeneration is
`410a318dc4d0c0a1167038f086d209409b1890a9`.

All 22 catalogue static checks and 24 DOM checks pass after regeneration. The
734 source resource rows remain byte-identical; 123 Science routes and 134
Humanities resources remain discoverable in the candidate shelves. These counts
are discovery counts, not curriculum completion. The two Science and fourteen
Humanities routes without proven terms remain explicitly unspecified.

## Remaining release requirements

1. The root hub adds links to `Science_Teesside/index.html` and
   `Humanities_Teesside/index.html`, but neither has a source catalogue row yet.
   `tools/verify_hub_catalogue.py` correctly rejects both. Add the two genuine hub
   rows while proving all original rows and their order are unchanged; prevent a
   shelf from including itself in its subject selection.
2. The cross-estate static contract still rejects the new root wording and the
   single Humanities browse paragraph. Use exact once-only insertion preservation,
   with negative controls for changed existing wording, changed destinations,
   duplicate insertions and modified/deleted unrelated lessons. Do not exempt
   arbitrary directories or weaken canonical-byte, brand, manifest or W7 checks.
3. The manifest pinning tool requires the Lessons and Apps verifier copies to be
   synchronized. This recovery has not changed either verifier or the Apps
   repository. The live copies already differ in their Lessons-only AAV-NIGHT
   record-path allowance; account for that explicitly in a coordinated repair.
4. Re-run exact-head CI and review real catalogue browser/print evidence after
   those fixes. The existing static/DOM reports do not establish visual acceptance.

The original drafts, finished native Office packs and dirty donor workspace are
preserved. No new lesson authoring, W7 changes, Games/Apex Kick/Skybreak changes,
or terminal Easter completion is claimed here.
