name: Education Pages publication
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
    # Advanced 2026-09-15 (CX2 R5) c06526c5 -> 297ed5de, a one-file Site carrier that is
    # c06526c5 byte-identical except the all-file admission registry, which now admits the
    # two derived catalogue files Lessons #539 changed (assets/catalogue/lesson-order.json,
    # assets/catalogue/terms-and-styles.json) beside their published rollback digests.
    # Advanced 2026-09-15 (CX2 S3) 297ed5de -> a89e2c60, the same carrier branch two registry
    # commits on: the return-week BUILD W8B lesson with its lesson-order, and the teaching-pack
    # hub relabelled as a lightweight offline edition. Each entry keeps its published rollback
    # digest beside the candidate, so the carrier admits the current served tree either way.
    # Publisher code is unchanged.
    # Advanced 2026-09-15 (CX2 §2 Friction) a89e2c60 -> 8cfd08b3, the same carrier branch, one registry commit on:
    # the GROW W3 Friction candidate (this branch, #538) admitted beside its published rollback digests — the lesson, resource pages, GROW pack records, seven Week 3 archives, W3A/W3B natives, the hub index, lesson-order.json and the usage registry (30 pairs, nothing added or removed). Digests from a full three-repository build at this builder, qualified by a control build of main that reproduces all 4044 admitted digests. Publisher code is unchanged.
    # Advanced 2026-09-15 (CX2 §2 Friction rows) 8cfd08b3 -> 17e82d70, the same carrier branch, one registry commit on:
    # the reviewed Science download rows (science-download-usage-additions.json, pinned by check_education_separation.py) brought to the nine Friction files whose size changed; the first publication of main acae624f stopped there after admission had passed. Registry unchanged from 8cfd08b3; no other publisher code moves.
    # Advanced 2026-09-15 (CX2 §3 Diffusion) 17e82d70 -> 7f97cbbf, the same carrier branch, one registry commit on:
    # the LAUNCH W4L1 Diffusion candidate (this branch, #546) admitted beside the published Friction digests (38 pairs, nothing added or removed) with its thirteen download rows reviewed; one commit on from the Friction rows carrier main pins. Publisher code is unchanged.
    # Advanced 2026-09-15 (CX2 §4.8 Sugar R10) 7f97cbbf -> d76182f4, the same carrier branch, one registry commit on:
    # the Sugar R10 hygiene candidate (this branch, #547) admitted beside the published Diffusion digests: the BUILD W8A lesson whose feedback card no longer names the evidence product, its re-rendered teacher DOCX and PDF, the BUILD checksum list and lesson-order.json (5 pairs, nothing added or removed). No reviewed download row moves — the two documents keep their KB-rounded sizes, measured at 0 of 320 rows changed. Digests from a full three-repository build qualified by a control build of main a8b3c968 that reproduces every admitted digest. Publisher code is unchanged.
    # Advanced 2026-09-15 (CX2 §8.3 Lane D) d76182f4 -> 429dd4ec, the same carrier branch, one registry commit on:
    # the whole return week (this branch, #545, carrying #543 BUILD and #544 GROW) admitted beside the published Sugar digests: BUILD W8B, GROW W8A and W8B, LAUNCH W8L1-W8L3 and lesson-order.json (7 pairs, nothing added or removed). No reviewed download row moves — these six lessons carry no pack downloads, measured at 0 of 320 rows. This carrier also replaces the withdrawn sparse W8B digest with the one a full build actually produces. Digests from a full three-repository build qualified by a control build of main ec7d34ab that reproduces every admitted digest. Publisher code is unchanged.
    # Advanced 2026-09-15 (CX2 §8.3 Lane D, corrected) 429dd4ec -> dd9d228a, the same carrier branch, one registry commit on:
    # the whole return week (this branch, #545, carrying #543 and #544) admitted beside the published Sugar digests, re-cut after the authored lesson-config block was restored to all six lessons (W8B b67be20d -> 492a4c28). Seven pairs, nothing added or removed; no reviewed download row moves. Digests from a full three-repository build qualified by a control build of main ec7d34ab. Publisher code is unchanged.
    # Advanced 2026-09-16 (CX2 §8.3 Lane D, corrected again) dd9d228a -> 3032ae71, the same carrier branch, one registry commit on:
    # the whole return week (this branch, #545, carrying #543 and #544) admitted beside the published Sugar digests, re-cut after the guidance button was docked and the stage timer moved off the home link in all six lessons (the full offline-pack check now passes 12/12). Seven pairs, nothing added or removed; no reviewed download row moves. Digests from a full three-repository build qualified by a control build of main ec7d34ab. Publisher code is unchanged.
    # Advanced 2026-09-16 (CX2 §5.3 EDU-Q1 companions) 3032ae71 -> 40339776, the same carrier branch, one registry commit on:
    # Carrier 40339776 admits the six catalogue/discovery outputs the companion rows move, as [served, candidate] from full builds (served oracle main e1a05dce).
    # Advanced 2026-09-16 (CX2 §5.3 EDU-Q1 companions (second mint)) 40339776 -> 7491329d, the same carrier branch, one registry commit on:
    # Carrier admits hub.js (pathway directory read case-insensitively) beside the six catalogue/discovery outputs, as [served, candidate] from full builds.
  publication:
    # Pinned 2026-09-07 reviewed Glitch handoff and all-file admission publisher (Site PR #292); this workflow reads
    # this repository's current source commit and never edits source content.
    # Advanced 2026-09-09 (LF1) 94ae15f8 -> 2e4d9966, a surgical Site commit that is
    # 94ae15f8 byte-identical except one review record: the all-file admission registry,
    # which now admits the 22 lessons and 8 SHA256SUMS.txt the LF1 week-label restoration
    # changed (#468), as 30 transition pairs. education-publication.yml itself is unchanged
    # between the two, and no builder, Play or usage-discovery code moves.
    # Advanced 2026-09-09 (FIN3 §4) 2e4d9966 -> 2a154e33, again surgical: measured, the
    # tree differs from 2e4d9966 in exactly one path, that same review record, and
    # education-publication.yml is byte-identical across the two, so the gate itself
    # does not move. It admits the four Science manifests whose bytes two Lessons
    # merges changed without a registry move -- Ruling 1 (#475) and Ruling 2
    # (aee37a1f) -- as transition pairs, and Ruling 3b's three new whole-route
    # manifests (#479) as ARRIVING. Those four are exactly what the publisher named
    # when it blocked publication on main tonight.
    # Advanced 2026-09-10 (AMEND-3R-GC1 P6) 2a154e33 -> 2e49afdd. This one is NOT
    # registry-only, and saying so is the point: it moves builder code as well.
    # .sb3 was missing from the publisher's extension allowlist, so all 23 Scratch
    # projects in the GROW Computing unit were dropped from the built tree by
    # extension while the source tree showed their 15 sibling links resolving. The
    # commit adds .sb3 to PUBLIC in build_education.py and to INERT in the admission
    # census, and extends the disguised-code control to it -- 58 controls pass, the
    # sb3 one planted-FAIL and restored-PASS on all three trees. Beside that it
    # admits the unit: 163 education-lessons paths ARRIVING at built bytes, plus
    # transition pairs for ICT/Teaching_Packs/index.html and the site
    # data/usage-registry.json. Measured against both the currently pinned 2a154e33
    # and Thursday's carrier ee56d2c7: 0 paths lost, 0 digests lost -- a strict
    # widening of both, not a sibling. education-publication.yml is byte-identical
    # to the pinned revision, so the gate itself does not move. Proven green in both
    # directions before pinning: a build of Lessons main (unit absent) and a build of
    # Lessons main + #493 (unit present) each report 0 admission problems.
    # GC1 registration repair, 12 September 2026: carrier based directly on 810ae8f8.
    # Only the reviewed teaching download rows and separation checker differ:
    # 128 Computing additions, every previous row retained, conditional on the unit
    # hub being installed. Builder, admissions, Science baseline and other inputs
    # remain unchanged. Content #493 is already merged; catalogue is separate.
    # P6 catalogue admission, 12 September 2026: four exact old/new output pairs
    # in the registry only. No executable builder, source selection, Science
    # baseline, size-table derivation or existing admission is replaced.
    # SW2-T5R1: registry-only carrier; publisher code and companion source pins unchanged.
    # SW2 Part T candidate: scoped owner chrome; companion inputs and workflow logic unchanged.
    # Part T completion approved 2026-09-13: current owner palette/chrome and exact rollback admissions.
    # Companion inputs and workflow logic remain unchanged; release follows verified Site root assets.
    # Part L: five exact owner-output admissions; executable publisher and companion pins unchanged.
    # NAV-2: complete public links on every owned shared header; pupil/account rules stay intact.
    # Six HTML headers and four derived size measurements change; companion inputs and workflow logic do not.
    # EDU-D2: registry-only carrier; six title display files plus guarded map, current rollback retained.
    # Advanced 2026-09-16 (CX2 section 7.1 PLAY-Q1 batch 2) 7491329d -> 08d74766, the same carrier branch:
    # Carrier admits the two re-stamped education lessons, the lesson-order projection of their
    # digests and the resource size table, as [served, candidate] from full builds; it also carries
    # the Play evidence rebound to the batch 2 bytes, without which this publisher refuses the
    # lessons as stale control/content evidence.
    # CARRIER PIN MOVE under release-red precedence, 2026-09-19, ORDER SX3-PASSES PASS 4 (3b)
    # follow-through: 5bd7702b -> a1b2a85c (Site #411). Site #411 admits the 3b-regenerated
    # data/resource-sizes.json digest that Lessons main 124f513b publishes (STOP-R on
    # Education Pages publication 35467537946, "CHANGED education-lessons/data/resource-sizes.json")
    # and holds both Site pins EQUAL TO LESSONS MAIN. Nothing else moves. Derived with
    # ls-remote against refs/heads/main, never typed; the caller digest below is re-cut
    # from these edited bytes into both gate copies, byte-identical.
    # Moved 2026-09-20 (ORDER FINISH-3, STANDING RULE L33: the carrier bump belongs on the
    # FIRST Lessons PR after a Site merge): fa4aa9e7 -> 3ed4577f, the squash merge of Site
    # #414 -- the 663-row reviewed download list, 1750 [digest, ARRIVING] admissions and both
    # pin pairs moved to 5e496ba5 / 2a9cb8e8. Red #3 (run 35516857985) proved that a
    # publication at the old carrier reads the old registry, so a re-dispatch alone could
    # never close the window; this line is the closer. SHA read with git rev-parse, never typed.
    # Moved 2026-09-20 (ORDER HUB-1, STANDING RULE L33: the carrier bump belongs on the
    # FIRST Lessons PR after a Site merge): 3ed4577f -> 5a039ae1, the squash merge of Site
    # #415 -- the Humanities hub's seven served paths admitted (four transition pairs, two
    # ARRIVING, the usage-registry pair), the reviewed download list 663 -> 825, and both pin
    # pairs moved to 9e8a947a / f374e8cc. The publication at the old carrier reads the old
    # registry (window opened at a16505ce on shelf-base.css, widened to seven at 9e8a947a), so
    # this line is the closer. SHA read with git rev-parse, never typed.
    # Moved 2026-09-20 (ORDER HUM-T batch 1, STANDING RULE L33: the carrier bump belongs on the
    # FIRST Lessons PR after a Site merge): 5a039ae1 -> cac643be, the squash merge of Site
    # #416 -- the six transplanted BUILD decks' served bytes admitted as transition pairs with the
    # lesson order and resource sizes, and both pin pairs moved to 3887f7ac / 327da98e. The
    # publication at the old carrier read the old registry (admission red 35541047709), so
    # this line is the closer. SHA read with git rev-parse, never typed.
    # Moved 2026-09-20 (ORDER HUM-T batch 1, second window, STANDING RULE L33: the carrier bump belongs on the
    # FIRST Lessons PR after a Site merge, and a carrier changes nothing the publication serves):
    # cac643be -> a461a77a, the squash merge of Site #417. SHA read with git rev-parse, never typed.
    # Moved 2026-09-20 (ORDER SCI-COMPLETE PASS A, STANDING RULE L33: the carrier bump belongs on the
    # FIRST Lessons PR after a Site merge, and a carrier changes nothing the publication serves):
    # a461a77a -> 3cff4ba7, the squash merge of Site #418. SHA read with git rev-parse, never typed.
    # Moved 2026-09-21 (ORDER HUM-T batch 2, STANDING RULE L33: the carrier bump belongs on the
    # FIRST Lessons PR after a Site merge, and a carrier changes nothing the publication serves):
    # the publisher pin 3cff4ba7 -> 297e09b4, pinned to the squash merge of Site #419 (instrument correction #16: a
    # naming word sits inside every pin literal's census window). SHA read with git rev-parse, never typed.
    # Moved 2026-09-21 (ORDER HUM-T batch 3, STANDING RULE L33: the carrier bump belongs on the
    # FIRST Lessons PR after a Site merge, and a carrier changes nothing the publication serves):
    # the publisher pin 297e09b4 -> 83ffbe8b, pinned to the squash merge of Site #420 (instrument correction #16: a
    # naming word sits inside every pin literal's census window). SHA read with git rev-parse, never typed.
    # Moved 2026-09-21 (ORDER HUM-T batch 4, STANDING RULE L33: the carrier bump belongs on the
    # FIRST Lessons PR after a Site merge, and a carrier changes nothing the publication serves):
    # the publisher pin 83ffbe8b -> aad258a9, pinned to the squash merge of Site #421 (instrument correction #16: a
    # naming word sits inside every pin literal's census window). SHA read with git rev-parse, never typed.
    # Moved 2026-09-21 (ORDER HUM-T batch 5, STANDING RULE L33: the carrier bump belongs on the
    # FIRST Lessons PR after a Site merge, and a carrier changes nothing the publication serves):
    # the publisher pin aad258a9 -> ec58a420, pinned to the squash merge of Site #422 (instrument correction #16: a
    # naming word sits inside every pin literal's census window). SHA read with git rev-parse, never typed.
    # Moved 2026-09-21 (ORDER HUM-T batch 6, STANDING RULE L33: the carrier bump belongs on the
    # FIRST Lessons PR after a Site merge, and a carrier changes nothing the publication serves):
    # the publisher pin ec58a420 -> 8e95534e, pinned to the squash merge of Site #423 (instrument correction #16: a
    # naming word sits inside every pin literal's census window). SHA read with git rev-parse, never typed.
    # Moved 2026-09-21 (SCI-COMPLETE PASS F, STANDING RULE L33: the carrier bump belongs on the
    # FIRST Lessons PR after a Site merge, and a carrier changes nothing the publication serves):
    # the publisher pin f29214ed -> 9600846c, pinned to the squash merge of Site #426 (instrument correction #16: a
    # naming word sits inside every pin literal's census window). SHA read with git rev-parse, never typed.
    # Moved 2026-09-22 (STOP-R2 explicit document tags, STANDING RULE L33: the carrier bump
    # belongs on the FIRST Lessons PR after a Site merge): 9600846c -> 25e613d9, the squash merge
    # of Site #427 -- the Summer 1 Humanities tree's first admission (260 arrivals, 10 pin
    # pairs), both L31 pin pairs moved to 1e668ee4 / ad806305 in BOTH domain-split-verify.yml
    # AND education-publication.yml (correction #22), and the registry baseline re-frozen on
    # the built bytes 5a3adcc3 -> a6e96f29 (977 -> 1105 retained rows, 128 joined, 0 removed,
    # 0 changed). The publication at the old carrier reads the old admission record, so this
    # line is the closer. SHA read with git rev-parse on the Site checkout, never typed.
    # Moved 2026-09-22 (title derivation, STANDING RULE L33: the carrier bump belongs on the
    # FIRST Lessons PR after a Site merge): 25e613d9 -> e52667a1, the squash merge of Site #428 --
    # the window that admits what Lessons #644 rebuilt. #644 moved two SERVED files
    # (Lessons/Science_Teesside/index.html and Lessons/data/resource-sizes.json) and merged
    # with no window, so the publication on Lessons main refused them by name (run
    # 35714663331). #428 moved both L31 pin pairs to 4f19ac9e / ecc78fff and re-cut the two
    # transition pairs to [what the old pin built, what the new pin builds]. THIS LINE IS THE
    # CLOSER: education-publication.yml resolves .sources/Site from builder_ref, so until it
    # moves the publication still reads the OLD admission record and reds identically.
    # SHA read with git rev-parse on the Site checkout, never typed.
          # Moved 2026-09-22 (Summer 1 responsive re-delivery, STANDING RULE L33 and the ruled
          # pending-then-land fold, STOP-RESP-2 step 2): e52667a1 -> aeb47509523dd0b7f8efb1c94f5eb550c17e2318,
          # the squash merge of Site #430 -- the window that admits this PR's 27 published paths as
          # [main-today, pending] transition pairs. THIS LINE IS THE CLOSER: education-publication.yml
          # resolves .sources/Site from builder_ref, so until it moves the publication still reads the
          # OLD admission record and refuses all 27 by name, as at run 35714663331. The bump is FOLDED
          # INTO this content PR rather than following it, so the publication reads the new registry on
          # its first run and no red is produced anywhere in the sequence. The purity check is scoped to
          # these two carrier lines: this PR deliberately moves 27 served paths, so the whole-diff form
          # of tools/hum/check_carrier_pure.py refuses it by design and is not the test here; step 3 is
          # the pure carrier. SHA read with git rev-parse on the Site checkout, never typed.
          # RE-BUMPED 2026-09-22 (STOP-RESP-4, route A): aeb47509 -> 0f93108616d196cc5869380ce390a6c8c34a8812, the
          # squash merge of Site #431. #430 admitted the 27 CONTENT movers, but regenerating the
          # size table produced a 28th, data/resource-sizes.json, which is itself served and which
          # #430 does not admit. The publication resolves .sources/Site from builder_ref, so this
          # line pointing at aeb47509 would have made THIS PR's own publication refuse that path by
          # name -- and step 3 could not repair it, because by then the publication has run.
          # STANDING RULE, added here: a carrier must point at the Site SHA that admits EVERY mover
          # in the pair, derived artefacts included, so the carrier bump is the LAST edit on the
          # Lessons branch, made only after every window it depends on has merged. SHA read with
          # git rev-parse on the Site checkout, never typed.
          # Moved 2026-09-22 (the Science hub's generation order, Matt's ruling on the S02 STOP;
          # the ruled pending-then-land fold): the publisher pin acb7bfa9 -> bb2836437e1fa8b4f3be836e37b748c75c86308c, the squash
          # merge of Site #433 -- the window that admits this PR's 3 moved served paths
          # (Science_Teesside/index.html, assets/catalogue/science-hub-bindings.json and the derived
          # data/resource-sizes.json) as [main-today, pending] transition pairs, measured from built
          # bytes after every derivation writer. Folded INTO this content PR so the publication reads
          # the new record on its first run. Bumped LAST, after the window merged (the standing rule
          # above). SHA read with git rev-parse on the Site checkout, never typed.
          # Moved 2026-09-22, the PURE CARRIER that closes the hub fold: the publisher pin
          # bb283643 -> dd9831f3530336b4623fb666d6a0834725cea0ce, the squash merge of Site #434 -- the EQUAL window
          # that discharged the four EQUAL pins onto Lessons main b1c70b0e (#655) and Apps main
          # 7a69b63d (#172). Nothing served moves in this change. Every hex literal in this carrier
          # now has a naming line within the census window (the lag control's C1). SHA read with
          # git rev-parse on the Site checkout, never typed.
          # Moved 2026-09-23 (PASS C Autumn 2 batch 1, rulings S1 and S3 on the S02 STOP; the ruled
          # pending-then-land fold): the publisher pin dd9831f3 -> c85c12c3d8dc09b4ef0b09ea62a01e61c61a8001, the squash
          # merge of Site #436 -- the window that admits this PR's 16 moved served paths (the 11
          # transplanted decks, the 3 re-cut Launch pack manifests, and the derived lesson order and
          # size table) as [main-today, pending] transition pairs, measured from built bytes. #436 is
          # the window's second cut: #435 (7be9a2b4) admitted the first, and the adversarial review then
          # confirmed two loop-adapter defects that moved the same 16 paths. Writers run first: the
          # re-stamp, lesson order, spine, census, size table and pack manifests. NOT run here: the
          # Science hub writer (route C, its own follow-on window) and the catalogue style writer (L39,
          # unruled), so the hub still under-claims NOT YET on these 11 until the follow-on lands.
          # Folded INTO this content PR so the publication reads the new record on its first run.
          # The pin is bumped LAST, after the window merged (the standing rule above). SHA read with
          # git rev-parse on the Site checkout, never typed.
          # Moved 2026-09-23 (the Science hub follow-on after PASS C batch 1, route C): the publisher
          # pin c85c12c3 -> 0fa7156565832ad6be227821b1e6c4bd7a67551a, the squash merge of Site #437 -- the EQUAL
          # window that also admits this PR's 3 moved served paths (the hub page, its bindings and the
          # derived size table) as [main-today, pending] transition pairs, measured from built bytes
          # after the hub writer and the size writer. Folded INTO this content PR so the publication
          # reads the new record on its first run. The pin is bumped LAST, after the window merged
          # (the standing rule above). SHA read with git rev-parse on the Site checkout, never typed.
          # Moved 2026-09-23, the PURE CARRIER that closes PASS C Autumn 2 batch 1's fold: the publisher
          # pin 0fa71565 -> 5251edc87e823275c42cea687ab29aa24f5430c1, the squash merge of Site #438 -- the EQUAL
          # window that discharged the four EQUAL pins onto Lessons main 3560dc0e (#663) and Apps main
          # 3b90f1a8 (#177). Nothing served moves in this change. SHA read with git rev-parse on the
          # Site checkout, never typed.
          # Moved 2026-09-23 (HUB1 v2, Autumn 2 hub badges; rulings R1-R3): the publisher pin
          # 5251edc8 -> 7d310e1e0dcfc6427f92efc7d586e004a80f9f16, the squash merge of Site #440 -- the window that admits
          # this PR's 6 moved served paths (the hub page, its bindings, the shelf, terms-and-styles, lesson
          # order and the size table) as [main-today, pending] transition pairs, measured from built bytes.
          # Folded INTO this content PR so the publication reads the new record on its first run. The pin
          # is bumped LAST, after the window merged. SHA read with git rev-parse on the Site checkout, never typed.
          # Moved 2026-09-23, the PURE CARRIER that closes HUB1 v2's fold: the publisher pin
          # 7d310e1e -> 9fd80be826d6d765268491279a7c881e57a7e88b, the squash merge of Site #442 -- the EQUAL window
          # that discharged the four EQUAL pins onto Lessons main 6d5a05ad (#667) and Apps main 2fd5621e
          # (#180), and fixed check_completion to read which weeks have a Teaching Packs section. Nothing
          # served moves in this change. SHA read with git rev-parse on the Site checkout, never typed.
          # Moved 2026-09-25 (Q12, the Science hub stylesheet; ORDER RS1-G3 section 1): the publisher pin
          # 9fd80be8 -> 3e5e489d190021a5690b3614fa5b53f174b04567, the squash merge of Site #444 -- the window that admits
          # this PR's 2 moved served paths (the hub page and the derived size table) as [main-today, pending] transition
          # pairs, measured from built bytes. Folded INTO this content PR so the publication reads the new record on its
          # first run. The pin is bumped LAST, after the window merged. SHA read with git rev-parse on the Site checkout, never typed.
          # Moved 2026-09-25, the PURE CARRIER that closes Q12's fold (ORDER RS1-G3 section 1): the publisher pin
          # 3e5e489d -> be9441d76cbc134ec0fc97cf83eb1ae39586a585, the squash merge of Site #445 -- the EQUAL window
          # that discharged the four EQUAL pins onto Lessons main 7aed39b3 (#673) and Apps main 045088e8
          # (#184). Nothing served moves in this change. SHA read with git rev-parse on the Site checkout, never typed.
          # Moved 2026-09-25 (LAND-A2 Science; Claude's rulings R1, R3 and R4): the publisher pin
          # be9441d7 -> 214749c0410a3a401c2d8fddb461c74bede9e196, the squash merge of Site #448 -- the window that admits
          # this PR's 210 served Science files as [digest, ARRIVING] and its 11 moved served paths as [main-today, pending]
          # transition pairs, and holds the separation fence as the transition pair a6e96f29 / 2969dfef, all measured from built
          # bytes. Folded INTO this content PR so the publication reads the new record on its first run. The pin is bumped
          # LAST, after the window merged. SHA read with git rev-parse on the Site checkout, never typed.
          # Moved 2026-09-25, the PURE CARRIER that closes LAND-A2 Science (ruling R6): the publisher pin
          # 214749c0 -> 079fba9990eda18979ef7c15abe1b95d648e866a, the squash merge of Site #449 -- window W2, which
          # refreshed search and provenance from Lessons c0b9b51f, collapsed the separation fence to 632100c6 and
          # discharged the five EQUAL pins onto Lessons main c0b9b51f (#677) and Apps main 1ad001c5 (#187). Nothing
          # served moves in this change. SHA read with git rev-parse on the Site checkout, never typed.
    uses: MattRoper1977/mattroper1977.github.io/.github/workflows/education-publication.yml@079fba9990eda18979ef7c15abe1b95d648e866a
    with:
      builder_ref: 079fba9990eda18979ef7c15abe1b95d648e866a
