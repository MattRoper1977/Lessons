name: Education Pages publication
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  publication:
    # Pinned 2026-09-07 reviewed Glitch handoff and all-file admission publisher (Site PR #292); this workflow reads
    # this repository's current source commit and never edits source content.
    # Advanced 2026-09-09 (SX1/SX2) 92abc460 -> 94ae15f8, a surgical Site commit that is
    # 92abc460 byte-identical except two review records: the all-file admission registry,
    # which now admits this repository's Spring/Summer Science batch (#454, #455), and the
    # retained usage-registry baseline it moves. education-publication.yml itself is
    # unchanged between the two, and no builder, Play or usage-discovery code moves.
    uses: MattRoper1977/mattroper1977.github.io/.github/workflows/education-publication.yml@94ae15f8d98ab9fe9d814aeb9bf417942452b83e
    with:
      builder_ref: 94ae15f8d98ab9fe9d814aeb9bf417942452b83e
