name: GROW LAUNCH v3 exact-source restaging

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  restaging:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Exact source bytes are being restaged as smaller independently hashed chunks."
