name: GROW LAUNCH v3 superseded transport diagnostic

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by glv3-closeout.yml after the staged XZ transport was proven corrupt."
