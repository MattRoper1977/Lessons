# DRAFT — append-only block for REGISTER.md (§5 R-G05 resolution) — HELD for Matt's key

Proposed to be appended to the existing `### R-G05` entry (original entry kept verbatim,
per append-only discipline). Not applied in this session.

---

- **RESOLVED AT HEAD by Pass E (KO Triage), `12cb6d9`.** Re-tested, not re-quoted (R-H07).
  **Method:** per ASDAN KO file, extract the KO key-word table terms and the We-Do-2
  `match-pill` terms, and count KO terms still drawn from the pills — the exact coupling the
  original 37/49 measured (KO print table generated from the game pills, gone stale when the
  pills' targets were rewritten). **Figures at HEAD (49 ASDAN KO files = 31 BUILD + 18 GROW):**
  **0/49** KO tables are still a full snapshot of the game pills; 13/49 share ≥1 term as
  legitimate common vocabulary (e.g. CAREERS_W3 `RELIABLE`/`EFFORT`); 36/49 share none.
- **Disposition:** the "37 of 49" is **retired by Pass G's rebuild (`9f657b6`), not left
  unverified.** Pass G decoupled the two artefacts — the KO is now an independently-authored
  real-vocabulary table; the We-Do-2 game keeps classification items (some deliberate wrong
  answers). At HEAD the coupling that made "KO disagrees with the game" a meaningful metric no
  longer exists (0/49 snapshots), so the figure is not re-derivable *as stated* — the question
  it asked was answered by the rebuild.
- **What remains live, and where it belongs:** whether each KO is stale against its **lesson**
  (not against the game) is the `ko_staleness`/Tier-1 question. All 49 ASDAN KO files appear in
  Pass E's 117-candidate set (via Pass F/O/W/W2 body-movers) and are triaged there — this
  resolution closes only the game-disagreement claim, not lesson-staleness.
