# PH-3 NON_DEPLOYMENT — what was deliberately NOT done

- **No filename renamed** — `PEQ_W6_Review_Progress_and_Sign_Off_the_Unit.html` stands; the
  title-vs-authority question is OPEN_ITEMS 42 with a redirect-plan requirement.
- **No workflow files added, anywhere, in either repo** — the PH-1 self-committing-loop lesson.
  Workflows on Lessons main remain the 12 from BASE.
- **No PDFs committed or regenerated** — C5 stopped at "no generator exists"; the two public
  PDFs are byte-untouched; stale sentences reported in the site repo's
  `asdan/_ph3/JOB_C_REPORT.md`.
- **The quarantined branch `pass-ph1-peq-hardening` (`ab7730c…`) untouched** — read twice,
  read-only, via `git show` only (§4.5 recovery + verification); never checked out, rebased,
  merged, or deleted.
- **No branch deleted** — Matt's UI job.
- **TEST COPY estates untouched** — `BUILD_Estate_v3/ GROW_Estate_v3/ LAUNCH_Estate_v3/`
  (OPEN_ITEMS 43), `ASDAN_Visual_Learning/`, `ASDAN_Lundy/`, `LundyLoop/`, `_glv3/ _sciv3/
  _lsg1/ _gsg1/ _bsg1/ _finish/ reports/ Games/`, every non-ASDAN suite, `resources.json`,
  sitemap, site homepage.
- **`Planning/` untouched** — the LAUNCH year-plan xlsx was read with openpyxl values-only;
  its false 10-hour-on-ComSk1 claims are reported with proposed cell fixes
  (`JOB_A_REPORT.md`), not edited.
- **A4 / C7 sentence not applied** — no `P8: GO` in the session; proposed text recorded
  (OPEN_ITEMS 47).
- **No merge** — no `GO-MERGE` in the session; both branches pushed, PRs opened, pass HOLDS.
- **ASDAN Moderation Lab not consolidated** — two deliverables exist (v5.2 base + five
  grafts); OPEN_ITEMS 49.
- **Sign-off wording beyond the seven verified A5 lines: report-only** — ~86 further
  occurrences classified with proposals in `SIGNOFF_CENSUS.md`, none edited.
- **The ~24 sibling BUILD "Witness: staff sign off…" lines in W1–W5/W7 lessons: not edited**
  — same pattern as the A5 seven but not PH-1R-verified; proposed estate-wide in the census.
- **The ≈145 other li-box-chassis decks (Art/Science/Humanities/other suites): untouched** —
  guidance-toggle rollout beyond the 85 is OPEN_ITEMS 50 with the measured estate map.
- **LAUNCH Careers / Community_Enterprise / Living_Independently START_HEREs: no panel** —
  measured as bank-labelling only, not qualification-route framing; recorded in
  `JOB_A_REPORT.md`.
- **`HANDOVER.md` was appended to via its own sanctioned path only** (a queue entry with its
  trigger). Its header forbids other appends and demands wholesale supersession by "the next
  session" — a Lundy-programme instruction that an ASDAN pass rewriting the file wholesale
  would have violated in spirit; the PH-3 "ledger" therefore lives inside queue entry 20.
  Recorded here as the §2-mandated contradiction report (prompt said "HANDOVER.md gets a
  PH-3 ledger"; the file's header said "do not append except the queue") — both sides are
  satisfied by the queue-entry form, neither silently obeyed against the other.
