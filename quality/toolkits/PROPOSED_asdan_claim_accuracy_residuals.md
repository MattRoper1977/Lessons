# PROPOSED · ASDAN claim-accuracy residuals — review-only

**STATUS: PROPOSED — review-only. This document edits nothing and authorises nothing. Any
claim edit it ever proposes is emitted as a PROPOSED DIFF under the claim-accuracy-only
doctrine (`_close/OPEN_ITEMS.md` #9 precedent: reframe the false claim, change no task, own
commit, independently revertable, Matt's word required) — applied nowhere by this pass.**
**OWNER: Matt · Pass LL-AS1 (rulings under Matt's delegation, 2026-08-05) · derived at `efc6cb3`.**
**Provenance: AUTHORED FRESH by Pass LL-AS1 — the pack's residuals doc
(`proposed_repo_files/quality/toolkits/PROPOSED_asdan_claim_accuracy_residuals.md`) and its
claim-accuracy patch (`patches/0004_asdan_claim_accuracy_proposal.patch`) were never received
in this workspace (R-LLS07). What that patch would have proposed is UNKNOWN and is not
inferred. This file records the derivable state at HEAD instead.**

**Defers to:** `quality/toolkits/CLAIMS_REGISTER.md` (the controlled vocabulary, the TK-1
census, the approved replacement wording) · `_passpq/SPEC_FACTS.md` (the PEQ spec facts of
record, R-TK02: a committed derivation outranks any brief's baked facts).

---

## The residual classes already named by the estate, re-derived at `efc6cb3`

Universe for each: tracked files under the three ASDAN trees (`BUILD_ASDAN/`, `GROW_ASDAN/`,
`LAUNCH_ASDAN/`) via `git grep`, except where stated. Derived at `efc6cb3`; re-run, never quote.

| residual class | closed by | derived now |
|---|---|---:|
| "Delivering a Project" (nonexistent CoPE-era unit) | R-K03 (T2-2) | **0 files** |
| ComSk1 "ten-hour window" over-claim (`ten hours|10 hours|10-hour`, GROW + LAUNCH trees) | R-K04 | **0 files** |
| doubled `ASDAN Studio · ASDAN Studio` label | R-RL01 (T2-1) | **0 files** |
| "predicted grade" on ASDAN surfaces | TK-1 census C-04/C-05 | **0 files** |
| "banked qualification" (whole repo) | CLAIMS_REGISTER wording table | **0 estate files** (1 hit = the register's own "do not write" column — documentation, not a claim) |
| certificate-banking in `Build/Slideshows/BUILD_L1_LI_Where_Money_Comes_From.html` | `feb108c` | **0 occurrences of `banked`** |
| UAS-as-qualification | **RETIRED FALSE at R-SEMH06 — do not re-raise** | not re-derived (retired) |

## The shipped ASDAN_Lundy layer's own census

The proposed toolkit layer shipped by this pass uses the six controlled states by name and
carries the honest-claims framing throughout. Derived at branch build:
`inspection-ready|banked qualification|predicted grade` = **0 files** in `ASDAN_Lundy/`.
The Studio's own JS refuses pupil wording that would raise a controlled state
(`certified|passed qualification|unit complete` without `centre-verified`) — the supplier
enforcing the estate's rule in its own tooling, recorded here because it is the direction of
travel this doctrine wants.

## PROPOSED DIFFS emitted by this pass

**One — applied nowhere.** Every residual class named by the estate derives to zero at
`efc6cb3`; the single new finding is in the pack layer this pass itself ships, caught by the
pass's adversarial red-line read:

### RES-AS1-01 · `ASDAN_Lundy/EVIDENCE_STATUS_AND_CLAIMS.html` — the Avoid list's escape clause scopes over never-reachable claims

The shipped line reads:

> **Avoid:** "qualification banked", "UAS grade", "unit certified", "passed" or "signed off"
> unless the authorised process has genuinely reached that state.

The trailing *"unless…"* grammatically scopes over the whole list, implying "UAS grade" and
"qualification banked" have a legitimately reachable state. They never do: **AQA UAS carries
no level and no grade** (R-SEMH06, retired-FALSE territory), and "qualification banked" is on
`CLAIMS_REGISTER.md`'s do-not-write list unconditionally. PROPOSED DIFF (one text node):

```
- unless the authorised process has genuinely reached that state.
+ — "unit certified", "passed" and "signed off" only when the authorised process has genuinely
+ reached that state; "qualification banked" and "UAS grade" never (AQA UAS carries no grade
+ and no level; see quality/toolkits/CLAIMS_REGISTER.md).
```

**Not applied** — the shipped file is a third-party pack file carried verbatim-plus-disclosed-
status-lines; a content reword is Matt's call under the doctrine above.

This document also exists so that the next ASDAN claim-accuracy finding has a home that is
review-only by construction, and so the absent pack patch is never retro-fitted from memory:
if the full pack ever arrives, its `0004` patch is checked against this file's method, not
merged on its own authority.

## Standing constraints on any future entry here

1. **Claim accuracy only.** The assessed task, timings, deliverables and pedagogy are out of
   scope; only the false claim moves (the R-K04 shape).
2. **Two-source agreement before a code is asserted** — spec + Evidence Binder (R-K03); where
   they are silent, the value is *"not yet mapped"* (never guess; OPEN_ITEMS #8).
3. **Emitted as a PROPOSED DIFF, applied nowhere**, own prospective commit, independently
   revertable, Matt's word the gate.
4. **Derive every count with its universe string** (R-G06); nothing in this file is quotable
   without re-derivation.
